# v11.3 — Genes/Entities Detail-Page Performance & UX Overhaul

**Date:** 2026-04-26
**Owner:** frontend
**Status:** spec — pending review (rev 2 after first-round review)

## 1. Problem

`/Genes/:symbol` (`app/src/views/pages/GeneView.vue`, 335 LoC) and `/Entities/:entity_id` (`app/src/views/pages/EntityView.vue`, 419 LoC) both fan out 8–10 API requests on mount and gate the entire page on a single `<BSpinner v-if="loading">`. The result: the user's curated SysNDD data (Associated Entities) paints **after** the slowest external lookup chain, the page feels sluggish, the layout wastes ~50 % of horizontal real estate at desktop widths, and several cards render an empty "no data" state instead of collapsing.

This spec covers the design contract for v11.3. Implementation plan(s) follow in `.planning/superpowers/plans/`.

## 2. Baseline measurements (2026-04-26)

Captured locally via Playwright over `make dev`, R API cache cleared between cold passes. See `.planning/perf/baseline-5-genes-parallel.json` and `.planning/perf/baseline-5-genes-fullnav.json` for the raw data.

### 2.1 Backend wall-time floor (all 8 endpoints fired in parallel)

| Gene   | Total bytes | Longest req (ms, warm) | Entity req (ms, warm) |
|--------|-------------|------------------------|-----------------------|
| GRIN2B | 535 KB      | 455 (entity)           | 455                   |
| SCN2A  | 858 KB      | 583 (entity)           | 583                   |
| MECP2  | 682 KB      | 473 (entity)           | 473                   |
| SHANK3 | 327 KB      | 393 (entity)           | 393                   |
| IFT172 | 591 KB      | 477 (entity)           | 477                   |

**Median total wall-time floor: 473 ms warm.** The entity endpoint is the longest single request because it joins the cleaned ndd entity view to multiple lookups; gnomAD variants is the largest payload (up to 845 KB for SCN2A). **This 393–583 ms range is the binding constraint for "first row of entities" timing** — the front-end can remove gating waste but cannot beat the backend floor without prefetch / aggregation work that is explicitly out of scope for v11.3.

### 2.2 What the user feels today (full-nav, cold)

| Gene   | Header visible (ms) | Entities visible (ms) | Gap (ms) |
|--------|---------------------|-----------------------|----------|
| SCN2A  | 157                 | 689                   | 532      |
| MECP2  | 139                 | 661                   | 522      |
| SHANK3 | 127                 | 637                   | 510      |
| IFT172 | 128                 | 650                   | 522      |
| **Median** | **134**         | **656**               | **522**  |

The header band paints fast (~135 ms), but the **entities table — the curated data the user came for — lags by ~520 ms** because it is mounted only after `loadGeneInfo()` resolves and `nextTick` completes. External cards lag entities by another ~200–400 ms.

### 2.3 Code-level findings

1. `<div v-if="loading">spinner</div>` gates the whole page until both gene calls return (`GeneView.vue:4`, `EntityView.vue:3`).
2. Two gene lookups (`?input_type=hgnc` and `?input_type=symbol`) fire concurrently as a fallback even when the URL parameter is the canonical symbol (`GeneView.vue:266`). The second call is wasted on the common path.
3. `<TablesEntities>` only needs the URL `:symbol` (or `:hgnc_id` — see §4.2.1), but is mounted only after the gene API resolves because `:filter-input` is computed from `geneData.value[0].symbol` (`GeneView.vue:172`).
4. External composables fire after `nextTick`, then issue ad-hoc `axios.get` per source with no shared cache; back-nav from a sibling gene re-fetches everything.
5. `EntityView.loadEntityInfo()` (`EntityView.vue:381`) `await`s the five sub-resources **sequentially** (status → review → publications → phenotypes → variation). Switching to `Promise.all` is its own significant win.
6. Three nested fluid containers per section (`container-fluid > BContainer fluid > BRow > BCol`) accumulate ~30–40 px of stacked padding on every block, contributing to the sparse feel.
7. Sort-arrow visual noise at idle: `app/src/assets/scss/components/_tables.scss:168` already centralizes the both-arrows-stacked rule when `aria-sort='none'`. Fixing it is a small SCSS edit, not a new behavior.
8. Empty `Gene Constraint (gnomAD)` card always renders, even when its content is "No constraint data available for this gene".
9. **Already correct (do not regress):** `app/src/components/small/GenericTable.vue:8` already sets `stacked="md"` so `TablesEntities` (and every `GenericTable` consumer) collapses to per-row labelled stacks below `md`. Mobile table stacking is **not** an open issue.

## 3. Senior-designer rating of current state (baseline UX rubric)

Each dimension scored 1–10. Acceptance gate for v11.3 is **≥ 8/10 on every dimension** verified by the same rubric after closeout.

| # | Dimension | Baseline | Target | Why baseline scored where it did |
|---|-----------|---------:|-------:|----------------------------------|
| 1 | Visual hierarchy            | 5 | 8 | Gene name is body-text size; section heading "Associated" is visually larger than the gene name itself. |
| 2 | Information density         | 4 | 9 | ~50 % whitespace at 1440 px; empty Constraint card holds half a row to print "no data"; footer logos eat 80 px. |
| 3 | Loading states / perceived speed | 3 | 9 | Single full-page spinner blocks everything ~135 ms; entities table paints 520 ms after header; no skeletons, no progressive reveal. |
| 4 | Layout / use of space       | 4 | 8 | Vertical stack with asymmetric 6/6 grid; empty cards consume half a row instead of collapsing; nested-fluid wrappers pad every block. (Mobile *table* stacking is already correct via `GenericTable.vue:8`.) |
| 5 | Consistency                 | 6 | 8 | Section title sizes vary; idle sort indicator shows both arrows stacked (`_tables.scss:168` — fix existing, don't reinvent); resource pills and identifier badges have inconsistent visual weight. |
| 6 | Affordance / discoverability| 6 | 8 | Resource pills don't visibly invite click; tab content (Protein View / 3D / Gene Structure) doesn't preview what's behind each tab. |
| 7 | Accessibility               | 5 | 8 | Low-contrast small-caps labels; no `aria-live` for loading→loaded; no skip-to-entities landmark; no axe-clean assertion in CI. |
| 8 | Motion / feedback           | 4 | 8 | Content pops in; no transitions; no hover-confirm on pills; no copy-to-clipboard toasts visible from screenshot. |

**Overall baseline: 4.6 / 10. Target overall: ≥ 8.0 / 10 with no dimension < 8.**

The rubric is re-scored against fresh screenshots after the v11.3 closeout (Section 9). The wave is not closed until every dimension reaches 8, which is the binding gate.

## 4. Solution architecture

### 4.1 Data layer (no new dep)

- `app/src/composables/useResource.ts` — `useResource<T>(key, fetcher, opts)` returning `{ data, error, loading, isStale, refresh, abort }`. On mount: serve cached value (if any) immediately as `isStale`; revalidate in background; dedupe in-flight by `key`; abort on key change. Tracks `fetchedAt`.
- `app/src/stores/cacheStore.ts` — Pinia store wrapping a Map keyed by stable URL key with `{ value, fetchedAt, ttlMs }`. LRU bounded at 64 entries. TTL defaults: 60 s for "our" endpoints (gene record, entity sub-resources), 5 min for external endpoints (ClinVar, AlphaFold, MGI, RGD, UniProt).
- Per-source hooks landed in this wave:
  - `useGeneRecord(symbolOrHgncId)`
  - `useGeneClinVar(symbol)`
  - `useGeneAlphaFold(symbol)`
  - `useGeneUniProt(symbol)`
  - `useGeneMGI(symbol)`
  - `useGeneRGD(symbol)`
  - `useEntityRecord(entityId)`, `useEntityStatus(entityId)`, `useEntityReview(entityId)`, `useEntityPublications(entityId)`, `useEntityPhenotypes(entityId)`, `useEntityVariation(entityId)`
- The existing `useGeneExternalData` and `useModelOrganismData` are split into the per-source gene-* hooks (one source = one hook). The composables are removed once their last call site is migrated in W2.
- **`TablesEntities` is *not* refactored to a hook.** It is a heavyweight Options-API component with internal pagination/filter/sort/URL-sync state; it owns its own `loadData()` that calls `listEntities()` (`TablesEntities.vue:510, :612`). v11.3 keeps that ownership and instead gives the page a way to mount it earlier. There is no `useGeneEntities` hook in this plan.

### 4.2 Hydration order

#### 4.2.1 Route-param shape and entity filtering

`/Genes/:symbol` accepts both an HGNC id (`/Genes/HGNC:4586`) and a symbol (`/Genes/GRIN2B`). The entity API supports both filter shapes — `equals(symbol,GRIN2B)` and `equals(hgnc_id,HGNC:4586)`. (Verified: a curl against `/api/entity/?filter=equals(hgnc_id,HGNC:4586)` returns the same 2 GRIN2B entities as the symbol filter.)

The page derives the filter directly from the URL parameter shape, with no dependency on the gene record:

```ts
// in GeneView.vue
const routeParam = computed(() => route.params.symbol as string);
const entityFilter = computed(() =>
  /^HGNC:?\d+$/i.test(routeParam.value)
    ? `equals(hgnc_id,${routeParam.value})`
    : `equals(symbol,${routeParam.value})`
);
```

`<TablesEntities :filter-input="entityFilter" :disable-url-sync="true">` therefore mounts and fires its `loadData()` on the same tick as the page mounts — gating on the gene record is removed. (No URL canonicalization needed; the entity API tolerates both shapes.)

#### 4.2.2 Mount order

1. **Entities first.** `<TablesEntities>` mounts immediately on page mount with the URL-derived filter; its `loadData()` is the first network request. No page-level loading gate.
2. **Gene header in parallel.** `useGeneRecord(routeParam)` fires on the same setup tick. The header card renders a skeleton until it resolves; cold target: skeleton ≤ 100 ms; resolved header within backend floor (gene API is ~50–100 ms warm).
3. **External hooks in parallel.** All five external hooks fire on the same setup tick, each owning its card's loading state. No `nextTick` deferral.
4. **Cancellation.** When the route param changes mid-flight (back/forward between genes), in-flight requests for the old key are aborted via the `useResource` `AbortController`.

### 4.3 Skeletons + layout

- `app/src/components/ui/SectionCard.vue` — wrapper taking `:loading`, `:empty`, `:error`, slots for header/body/actions. Skeleton stripes while loading; renders nothing when `empty && !loading` (today's "No constraint data available" empty card disappears entirely). Stable height during skeleton phase to avoid CLS.
- Header band: gene badge + name + chromosome location + resources + identifiers in one wrapping flex row at ≥ xl breakpoint; stacks at smaller breakpoints. Resources and identifiers share the same badge component for visual consistency. Gene name becomes the largest text element on the page (lifts hierarchy score).
- Below header: Associated Entities (full width, unchanged size — it is the data-dense block).
- Below entities: 3-up `<BRow>` at `lg+` for `Constraint | ClinVar | ModelOrgs` cards. At `<lg` it stacks. Each card hides itself when its hook resolves to "no data".
- `GenomicVisualizationTabs`: lazy-mount inactive tab panels (today they all mount on load). `<KeepAlive>` so switching tabs is instant after first activation.
- Remove duplicate fluid wrappers: replace `container-fluid > BContainer fluid > BRow > BCol` triple-nesting with a single `BContainer fluid > BRow > BCol` per section.
- **Sort-arrow noise (existing fix):** edit `_tables.scss:168` so the `aria-sort='none'` state shows both arrows only on `:hover`/`:focus-visible`; idle renders neither (or the same neutral marker as before, faintly). No change to `BTable` props or `GenericTable.vue`.
- Footer: collapse the logo strip to a smaller footer row to recover ~40 px of below-the-fold real estate.

### 4.4 EntityView gets the same treatment

- The page mounts each section as its own `<SectionCard>` immediately on page mount; no page-level loading gate.
- Header card uses the same skeleton + `<SectionCard>` pattern with `useEntityRecord(entityId)`.
- `loadEntityInfo` is removed. Each subsection is owned by its own hook, all firing on the same setup tick:
  - `useEntityStatus(entityId)` → status badge / timeline card
  - `useEntityReview(entityId)` → review/synopsis card
  - `useEntityPublications(entityId)` → publications card (filters `publication_type === 'additional_references'`)
  - useEntityPublications result is reused for the gene-reviews card (filter `publication_type === 'gene_review'`); no second request
  - `useEntityPhenotypes(entityId)` → phenotypes card
  - `useEntityVariation(entityId)` → variation-ontology card
- The linked-gene block (showing the entity's HGNC gene) reuses `useGeneRecord(entity.hgnc_id)` once the entity record resolves.
- This explicitly fixes the existing sequential-await bug in `EntityView.loadEntityInfo` (Section 2.3 finding 5).

### 4.5 Motion + feedback (low-cost)

- `<Transition mode="out-in">` between skeleton and content, 150 ms fade.
- Hover scale (1.02) + shadow on resource pills to confirm clickability.
- "Copied!" inline toast on identifier copy.
- Number reveal on KPI badges (count-up over 200 ms when a card resolves).

### 4.6 Accessibility

- Add `aria-live="polite"` to the entities table region.
- Add a skip-link "Jump to associated entities" rendered visibly on focus.
- Replace small-caps gray labels with theme tokens that satisfy AA contrast.
- Run `axe-core` against the page in the perf bench (Section 9) and fail if there are new violations vs. baseline.

## 5. Out of scope

- No backend changes (Plumber worker count, response shape, server-side caching).
- No SSR / Nuxt migration.
- No GraphQL or aggregated endpoint.
- No new design system. Bootstrap-Vue-Next + existing tokens stay.
- No new runtime dependency. (See §9 — the harness uses `PerformanceObserver` only; `web-vitals` is **not** added.)
- The Ontology view, the Search view, and table list views are out of scope. They share *some* of the same patterns; the abstractions land here so those views can adopt later without a rewrite.
- `TablesEntities` internals (filter parsing, pagination, URL sync) — the wave only changes when/where it is mounted, not how it fetches.

## 6. Branch model

- Master branch: `master`.
- Integration branch: `feature/v11.3-genes-entities-perf-ux`.
- Wave branches: `feature/v11.3-w1-data-layer`, `feature/v11.3-w2-genes-page`, `feature/v11.3-w3-entities-page`, `feature/v11.3-w4-bench-polish`.
- Worktrees under `../sysndd-worktree-w{1,2,3,4}` so subagent lanes don't collide.
- Single integration PR to `master` once all four waves are merged into the integration branch.

## 7. Wave breakdown

### W1 — Data layer (no production-view code touched)

- `useResource` + Pinia `cacheStore`.
- Per-source hooks for the gene-side (5: gene record, ClinVar, AlphaFold, UniProt, MGI, RGD) and entity-side (6: entity record, status, review, publications, phenotypes, variation).
- **Not in W1:** the duplicate `?input_type=symbol` removal stays in W2 because it requires editing `GeneView.vue:266`. W1 ships only `useGeneRecord(symbolOrHgncId)` with the input-type discriminator built in; W2 wires the page to it.
- **Not in W1:** any `useGeneEntities` hook (per §4.1, `TablesEntities` keeps owning its fetch).
- Vitest: cache hit, miss, stale-while-revalidate, dedupe (concurrent calls → one fetch), abort on key change, LRU eviction.
- No change to `GeneView.vue` / `EntityView.vue` yet — old code paths keep working.

### W2 — Genes page

- Rewire `GeneView.vue` to use the per-source hooks.
- **Drop the duplicate `?input_type=symbol` lookup** (this removal lives in W2, not W1, because it touches `GeneView.vue`). The single discriminated call is delegated to `useGeneRecord(routeParam)`.
- Mount `<TablesEntities>` immediately on the URL-derived filter (§4.2.1); no page-level loading gate.
- Introduce `<SectionCard>` with skeletons.
- Apply 3-up grid; hide-when-empty.
- Lazy-mount tab panels in `GenomicVisualizationTabs`.
- Edit `_tables.scss:168` to fix idle-state both-arrows.
- Vitest: each section's skeleton state, hide-when-empty, error path; URL-shape filter selection (HGNC id vs symbol).
- Playwright: assert first row of entities table is visible **before** any external card resolves, on `/Genes/GRIN2B`, `/Genes/MECP2`, and `/Genes/HGNC:4586`.

### W3 — Entities page

- Rewire `EntityView.vue` to use the per-source entity hooks.
- Each subsection (status, review, publications, gene-reviews, phenotypes, variation) becomes its own `<SectionCard>` mounted on page mount; no page-level loading gate.
- **Removes the sequential-await bug** in `EntityView.loadEntityInfo` (the five awaits become five parallel hooks).
- Reuse `<SectionCard>` and the gene-record hook for the linked-gene block.
- Vitest: each section's skeleton/empty/error state.
- Playwright: assert the status card paints before publications (longest entity sub-resource) finishes, on `/Entities/304` and `/Entities/400`.

### W4 — Benchmark + polish

- `app/tests/perf/genes-entities.bench.spec.ts` Playwright harness — see Section 9.
- Tighten any regressions; ensure `axe-core` clean.
- Update `documentation/08-development.qmd` with the perf bench command.
- Re-rate the page against the rubric in Section 3 and commit the after-state scores; the wave does not close until every dimension is ≥ 8.

## 8. Targets (acceptance gates)

These are the **after-state** numbers v11.3 must hit. Measured by the Section 9 harness, on `make dev`, viewport 1440×900, R API cache cleared before cold passes, two genes (GRIN2B, MECP2) and two entities (sysndd:304, sysndd:400) per pass — every probe must hit the gate.

| Metric                                  | Baseline (median, cold) | Target | Notes |
|-----------------------------------------|-------------------------:|-------:|-------|
| Time to first useful paint (skeletons)  | n/a (none today)         | ≤ 150 ms | Skeleton frames visible in DOM. |
| Time to gene header skeleton visible    | 134 ms (real header)     | ≤ 100 ms | Skeleton paints first; header text fills in place. |
| Entity request **starts** at             | 397 ms after nav (gated) | ≤ 50 ms after nav | Mount-time fetch; URL-shape filter. Asserted by Performance API resource start. |
| Time to entities table first row (cold) | 656 ms                   | ≤ 700 ms (p50) **and** ≤ 250 ms after entity-API response arrives | The first half is the backend floor (≈ 470 ms median + render). The second half is the v11.3-controllable budget — render must not eat more than 250 ms after the network response lands. |
| Time to all sections settled            | ~1100 ms (estimated)     | ≤ 1500 ms (p95 cold) | Even if backend stays slow, no regression. |
| Total payload across all endpoints      | 530 KB median            | ≤ 530 KB | Drop duplicate `?input_type=symbol` recovers ~500 B per page; cap regression. |
| LCP (largest contentful paint)          | unmeasured               | < 2.5 s warm | Captured by `PerformanceObserver({ type: 'largest-contentful-paint' })`. |
| CLS                                     | unmeasured               | < 0.1 | Captured by `PerformanceObserver({ type: 'layout-shift' })`. Stable-height skeletons. |
| INP                                     | not gated                | not gated | Skipped — no reliable headless harness without `web-vitals` (and `web-vitals` is explicitly not adopted, see §5). Track manually in DevTools if regressions are reported. |
| Senior-designer rubric (Section 3)      | 4.6 / 10                 | ≥ 8 / 10 each dim | Re-scored against fresh screenshots at 1440 / 1024 / 375 px after closeout. |
| `axe-core` violations introduced        | n/a                      | 0      | Clean against baseline. |

If any target is missed, the wave is not closed; a follow-up issue captures the gap before merge.

## 9. Verification harness

`app/tests/perf/genes-entities.bench.spec.ts` is a Playwright spec that:

1. Reads a small fixture list of two genes (GRIN2B, MECP2) and two entities (sysndd:304, sysndd:400).
2. Calls `make cache-clear` via `node:child_process` (skip on CI; local-only) before the cold pass.
3. For each gene/entity, hard-navigates to the page and uses **`PerformanceObserver`** in `page.evaluate` for LCP and CLS, plus `performance.getEntriesByType('resource')` for entity-request start/end times. Asserts the targets in Section 8.
4. Polls the DOM for: skeleton frames present, first entity row, all spinners gone.
5. Runs `axe-core` (already a project dev dependency for accessibility) against the loaded page.
6. Writes timings to `.planning/perf/after-{date}.json` and a side-by-side diff vs. the baseline JSON in this PR.

The harness uses `PerformanceObserver` only — **`web-vitals` is not added** (per §5). INP is therefore not gated by the harness; LCP and CLS are.

The harness is **local-only** (per AGENTS.md, Playwright in this repo is local-only — there is no Playwright CI workflow). The acceptance numbers are committed in the perf JSON; PR review checks the diff.

## 10. Documentation contract

When v11.3 lands, update in the same change:

- `AGENTS.md` — add a "Perf benchmarks" subsection under "Stack-Specific Gotchas" pointing to `app/tests/perf/`.
- `documentation/08-development.qmd` — add the bench-spec command and the cache-clear step.
- This spec file — append a "Closeout" section with the after-state rubric scores and after-state perf JSON path.
- `app/src/views/pages/GeneView.vue` and `EntityView.vue` — file-top comment updated to reflect the new mount order.

## 11. Risks and mitigations

| Risk | Mitigation |
|------|------------|
| `useResource` SWR semantics drift from existing axios behaviour (interceptors, base URL, auth) | Build on top of the existing `apiClient` (`@/api/client`); inherit interceptors. |
| Stale cache surfaces wrong data after a privileged write (e.g., user updates an entity, reads stale view) | TTL is 60 s for our endpoints; mutations call `cacheStore.invalidate(prefix)` from the existing `useEntityMutations`. |
| `TablesEntities` filter-input may briefly mismatch on initial mount because `filterInput` becomes reactive on the URL param | The `entityFilter` computed reads only `route.params`, which Vue Router resolves before `setup()` returns; `TablesEntities` reads `filterInput` in `mounted()` so the value is stable by the time `loadData()` runs. Tested by the Playwright probe asserting "entity request starts ≤ 50 ms after nav". |
| Lazy tab content breaks cross-tab links (e.g., a click on a domain in Protein View jumps to Gene Structure) | Audit `GenomicVisualizationTabs` cross-tab refs in W2; if any panel publishes shared state, hoist that state into the parent before lazy-mounting. |
| Mobile stacked `BTable` regresses for power users on tablet | Already correct via `GenericTable.vue:8` (`stacked="md"`); no change in v11.3. Listed here so future contributors don't "fix" it. |
| Skeleton + content size mismatch causes CLS | Skeleton card minimum heights match the resolved content's typical heights; verified by the harness CLS gate. |
| Re-rate rubric is subjective | Lock the rubric text in this spec; re-score against the same screenshots at 1440 / 1024 / 375 px; require user-visible PR description to include the new screenshots side-by-side. |

## 12. PR shape

A single integration PR titled `feat(v11.3): genes/entities perf + UX overhaul`. Body includes:

- Before/after waterfall screenshots (5-gene panel).
- Before/after rubric scores (Section 3 table).
- Diff line totals broken down by wave.
- Link to perf JSON files.
- Closing keyword for the GitHub issue tracking this milestone (to be opened at plan-write time).

## 13. Verification greps for closeout

```bash
# No more page-level loading gate
grep -n "v-if=\"loading\"" app/src/views/pages/GeneView.vue   # → no match
grep -n "v-if=\"loading\"" app/src/views/pages/EntityView.vue # → no match

# Duplicate gene lookup gone (W2)
grep -n "input_type=symbol" app/src/views/pages/GeneView.vue  # → no match
grep -n "getGeneBySymbol" app/src/views/pages/GeneView.vue    # → no match

# SectionCard adopted
grep -rn "<SectionCard" app/src/views/pages/                   # → ≥ 5 hits per page

# useResource adopted
grep -rn "useResource(" app/src/composables/                   # → ≥ 11 hits (5 gene-side + 6 entity-side)

# Sequential await bug gone
grep -n "await getEntityStatus" app/src/views/pages/EntityView.vue  # → no match (replaced by hooks)

# Bench spec exists
test -f app/tests/perf/genes-entities.bench.spec.ts            # → present

# web-vitals NOT adopted
grep -n "web-vitals" app/package.json                          # → no match
```

## 14. Open questions

None blocking after rev 2. All six review items folded in:

1. Entity-table 250 ms target → reframed as "entity request starts ≤ 50 ms after nav" + "first-row render budget ≤ 250 ms after response" (Section 8). The backend floor (Section 2.1) is the binding constraint.
2. Route-param shape → resolved in §4.2.1; entity API accepts both `equals(symbol,…)` and `equals(hgnc_id,HGNC:…)`, so no gene-record dependency or URL canonicalization is needed.
3. W1 contradiction → duplicate-lookup removal moved to W2 (it touches `GeneView.vue`); W1 is now strictly a data-layer drop with no production-view edits (Section 7 W1).
4. `useGeneEntities` vs `TablesEntities` → no `useGeneEntities` hook ships. `TablesEntities` keeps its `loadData()`; the page just mounts it earlier (§4.1, §4.2.1, §5 out-of-scope).
5. EntityView sections → §4.4 names the actual sub-resources (`useEntityStatus`, `useEntityReview`, `useEntityPublications`, `useEntityPhenotypes`, `useEntityVariation`); §2.3 finding 5 documents the sequential-await bug; W3 (Section 7) explicitly removes it. No `<EntityHistoryTable>` reference anywhere.
6. `web-vitals` → not adopted. Harness uses `PerformanceObserver` only (§5, §9). INP is not gated (Section 8).
