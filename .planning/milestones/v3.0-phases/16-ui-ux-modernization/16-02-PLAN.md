---
phase: 16-ui-ux-modernization
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - app/src/assets/scss/components/_cards.scss
  - app/src/assets/scss/custom.scss
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Cards have softer shadows with hover elevation effect"
    - "Card corners are rounded using design token"
    - "Section spacing is consistent using spacing tokens"
    - "Dark card headers have proper contrast"
  artifacts:
    - path: "app/src/assets/scss/components/_cards.scss"
      provides: "Card styling with shadows and rounded corners"
      contains: ".card"
  key_links:
    - from: "app/src/assets/scss/components/_cards.scss"
      to: "partials/_shadows.scss"
      via: "CSS custom properties"
      pattern: "var\\(--shadow-"
---

<objective>
Implement modern card and container styling with softer shadows and improved spacing.

Purpose: Cards are the primary content containers in the application. Modernizing their appearance with subtle shadows, rounded corners, and consistent spacing creates the "modern scientific aesthetic" specified in CONTEXT.md.

Output: Card styling SCSS partial that enhances Bootstrap card appearance while maintaining compact density for data-heavy views.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-ui-ux-modernization/16-CONTEXT.md
@.planning/phases/16-ui-ux-modernization/16-RESEARCH.md
@.planning/phases/16-ui-ux-modernization/16-01-SUMMARY.md
@app/src/views/Login.vue
@app/src/components/tables/TablesGenes.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create card styling partial with shadow elevation</name>
  <files>
    app/src/assets/scss/components/_cards.scss
    app/src/assets/scss/custom.scss
  </files>
  <action>
    Create `app/src/assets/scss/components/` directory if not exists.

    Create `components/_cards.scss` with modern card styling:

    1. Base card styles:
       - border-radius: var(--radius-md) (0.375rem)
       - box-shadow: var(--shadow-sm)
       - transition: box-shadow var(--transition-base)
       - border: 1px solid rgba(0, 0, 0, 0.08) (subtle border)

    2. Card hover state (optional enhancement):
       - box-shadow: var(--shadow-md) on hover
       - Only apply to .card-interactive class (not all cards)

    3. Card header refinements:
       - Improve .card-header padding for compact density
       - Ensure dark variant has proper contrast
       - border-radius: var(--radius-md) var(--radius-md) 0 0

    4. Card body spacing:
       - Use spacing tokens for consistent padding
       - Support .card-body-compact class for data-heavy views

    5. Update custom.scss to import components/_cards.scss

    Add @media (prefers-reduced-motion: reduce) to disable hover transitions.
  </action>
  <verify>
    - app/src/assets/scss/components/_cards.scss exists
    - File contains var(--shadow-sm) and var(--radius-md)
    - Build succeeds: `npm run build --prefix app`
  </verify>
  <done>
    Card styling partial created with shadow elevation, rounded corners, and compact density support
  </done>
</task>

<task type="auto">
  <name>Task 2: Add container and section spacing utilities</name>
  <files>
    app/src/assets/scss/utilities/_spacing.scss
    app/src/assets/scss/custom.scss
  </files>
  <action>
    Create `app/src/assets/scss/utilities/` directory if not exists.

    Create `utilities/_spacing.scss` with spacing utility classes:

    1. Section spacing classes:
       - .section-compact { padding: var(--spacing-compact); }
       - .section-base { padding: var(--spacing-base); }
       - .section-comfortable { padding: var(--spacing-comfortable); }

    2. Gap utilities (for flex/grid):
       - .gap-compact { gap: var(--spacing-compact); }
       - .gap-base { gap: var(--spacing-base); }

    3. Container refinements:
       - .container-compact for data-heavy layouts
       - Tighter margins on mobile

    4. Update custom.scss to import utilities/_spacing.scss
  </action>
  <verify>
    - app/src/assets/scss/utilities/_spacing.scss exists
    - Build succeeds: `npm run build --prefix app`
    - CSS contains .section-compact class
  </verify>
  <done>
    Spacing utilities created for consistent section and container spacing
  </done>
</task>

</tasks>

<verification>
```bash
# Build succeeds
npm run build --prefix app

# Check card styles in output
grep -r "\.card" app/dist/assets/*.css | head -5

# Verify shadow tokens are used
grep -r "var(--shadow-" app/dist/assets/*.css | head -3

# Start dev server and visually check a card (Login page)
npm run dev --prefix app &
sleep 3
echo "Check http://localhost:5173/Login for card styling"
kill %1
```
</verification>

<success_criteria>
- [ ] components/_cards.scss created with modern card styling
- [ ] Cards use shadow tokens (--shadow-sm, --shadow-md)
- [ ] Cards use radius tokens (--radius-md)
- [ ] Spacing utilities created (.section-compact, etc.)
- [ ] Hover transitions respect prefers-reduced-motion
- [ ] Build succeeds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-ui-ux-modernization/16-02-SUMMARY.md`
</output>
