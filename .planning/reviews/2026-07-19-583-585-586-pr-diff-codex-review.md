No P0 blockers found. I found four P1 items and one P2 test gap.

### P1 — generator provenance omits active algorithm parameters — CONFIRMED

[analysis-snapshot-provenance-generator.R:93](/home/bernt-popp/development/sysndd/api/functions/analysis-snapshot-provenance-generator.R:93) records functional params without Leiden `beta = 0.01` or `n_iterations = -1`, despite both being active in [analyses-functions.R:234](/home/bernt-popp/development/sysndd/api/functions/analyses-functions.R:234). It also omits the phenotype supplementary-variable configuration from [analysis-snapshot-provenance-generator.R:109](/home/bernt-popp/development/sysndd/api/functions/analysis-snapshot-provenance-generator.R:109), although the actual call fixes `quali.sup = 1:1` and `quanti.sup = 2:4`.

Fix: include these values in `algorithm.params` and add regression assertions. This is required for the #585 stated reproducibility contract; it does not affect hashes.

### P1 — crosswalk guard skips passthrough rules — CONFIRMED

[api/tests/testthat/test-unit-comparisons-crosswalk.R:23](/home/bernt-popp/development/sysndd/api/tests/testthat/test-unit-comparisons-crosswalk.R:23) explicitly `next`s every `passthrough` rule. Thus the consistency guard does cover G2P casing, SFARI `NA`, NDD GeneHub fallback, and Radboud all-values, but not SysNDD, OMIM-NDD, or Orphanet passthrough rules declared at [category-normalization.R:153](/home/bernt-popp/development/sysndd/api/functions/category-normalization.R:153).

Fix: for `passthrough`, probe a representative category and assert output equals the probe; do not compare it to the crosswalk’s intentionally null `normalized_tier`.

### P1 — frontend crosswalk contract is incomplete — CONFIRMED

[app/src/api/comparisons.ts:150](/home/bernt-popp/development/sysndd/app/src/api/comparisons.ts:150) defines `ComparisonsCrosswalk` without `non_tier_fillers`, which is always emitted by [category-normalization.R:166](/home/bernt-popp/development/sysndd/api/functions/category-normalization.R:166). Runtime behavior is unaffected because the component does not consume it, but the typed client does not exactly mirror the endpoint as required.

Fix: add a `CrosswalkNonTierFiller { value; meaning }` type and `non_tier_fillers: CrosswalkNonTierFiller[]`.

### P1 — dialog ARIA declaration disagrees with rendered popover role — CONFIRMED

[EvidenceTierMappingHelp.vue:19](/home/bernt-popp/development/sysndd/app/src/components/analyses/EvidenceTierMappingHelp.vue:19) declares `aria-haspopup="dialog"`, but its [BPopover at line 25](/home/bernt-popp/development/sysndd/app/src/components/analyses/EvidenceTierMappingHelp.vue:25) renders BootstrapVueNext’s default `role="tooltip"`. It contains a keyboard-focusable link, so this is a screen-reader semantic mismatch.

Fix: pass `role="dialog"` plus an accessible label/labelled-by relationship to `BPopover`, or use a component whose dialog semantics are native.

### P2 — interaction test stubs away the actual popover behavior — CONFIRMED

[EvidenceTierMappingHelp.spec.ts:29](/home/bernt-popp/development/sysndd/app/src/components/analyses/EvidenceTierMappingHelp.spec.ts:29) replaces `BPopover` with a static `<div>`. The test verifies component state and Escape handling, but not BootstrapVueNext’s target-click/v-model synchronization or actual Tab navigation. The production implementation itself is sound: the open-only handler at [EvidenceTierMappingHelp.vue:101](/home/bernt-popp/development/sysndd/app/src/components/analyses/EvidenceTierMappingHelp.vue:101) does not race with BVN’s later native click listener; second click closes correctly.

Fix: add a browser-level interaction test using the real popover: Enter/Space opens, Tab focuses the crosswalk link, Escape closes and returns focus.

Clean / confirmed:

- #583 mapping is correct: PanelApp `3/2/1 → Definitive/Moderate/Limited`; no PanelApp rule maps to Refuted. G2P disputed/refuted remains Refuted.
- `/metadata` has `mapping_version` in all three paths; browse meta carries it; XLSX includes it through the exported `meta` worksheet.
- The per-source priority already ranks Moderate above Limited, so Amber’s new precedence is intentional; no conflicting consumer was found.
- `/crosswalk` uses `na="null"`; SFARI native `NA` serializes to JSON `null`.
- API/client endpoint paths align. The response is a named JSON object, not a scalar Plumber array; `apiClient.get()` is correctly used without a second `.data` unwrap.
- #585 hash boundaries hold: `applied_params` is an attribute, generator is manifest-only, payload/input hashes exclude it, and release identity projects only scientific layer fields. Release provenance is correctly per-layer and excluded from `content_digest`.
- Production loading is correct: provenance loads before the builder, the fail-closed gate runs in normal boot, and `start_async_worker.R` initializes `version_json`.
- Manifest migration is correct: latest `046`, count `44`, nullable/idempotent column migration, and pins were updated. Repository insert has 24 columns, 23 placeholders, and 23 parameters; alignment is correct.
- No new handwritten source exceeds 600 lines; no relevant placeholder/dead-code markers found; no new `base::get` masking issue.
- #584 is absent, as required.

Validation limitation: `git diff --check` passed. `make code-quality-audit` and the targeted Vitest run could not start because this read-only environment disallows temporary-file creation (`EROFS`).
