# Codex 5.6-terra (high) — SPEC review: #583/#585/#586

Model: gpt-5.6-terra, effort high, read-only. Reviewed spec
`.planning/superpowers/specs/2026-07-19-comparisons-mapping-provenance-583-585-586-design.md`.

## Verdict
- #583 conditionally sound; exported/frozen comparison outputs must be self-identifying (mapping_version on browse+XLSX, not only /metadata).
- #585 hash-additivity sound but not implementation-ready: release representation (per-layer vs singleton generator) + concrete algorithm/library provenance contract must be resolved.
- #586 good direction but not yet compliant with no-drift (fallback restates tiers) and keyboard-accessibility (focus-triggered popover with a link is not Tab-navigable).
- Migration 046 / count 44 correct (repo at 045; 43 migrations).

## P0 (blocking)
1. Release provenance is per-layer, not a singleton `generator` (that block is the packager). Store each snapshot's generator under `source.snapshots[]`/`layers[]`; keep `generator` for the packager; add a release test with distinct per-layer generators. Document immutability: a pre-existing release is not retrofitted (idempotency early-return at release.R:377) — acceptable per #573 immutability, state it.
2. Make the generator contract reproducible, not a placeholder: define per-analysis-type params (functional: Leiden resolution/seed/beta/iterations/score threshold; phenotype: ncp/supplementary vars/kk=Inf/min-max/consolidation) + a fixed library allowlist/version resolver. Capture generated_at ONCE in UTC and pass the same value to generator_json and the manifest insert.
3. #586: fetch-failure fallback must NOT restate tiers/PanelApp policy (contradicts no-drift) — show a neutral "unavailable" message. focus+hover popover with an internal anchor is not keyboard-navigable; use click/keyboard toggle + Escape + interaction test (focus badge → Enter/Space → Tab to link → Enter). Placement after the Overlap h2 is correct.

## P1 (should fix)
1. Consistency guard: one-way row test is incomplete. Tag crosswalk rows with rule_kind (exact/case_insensitive/missing/fallback/passthrough) and test every executable behavior (Unclassified, arbitrary Radboud, G2P casing, SFARI NA, ndd_genehub catch-all). No full normalizer rewrite needed.
2. mapping_version: /metadata has THREE return paths (missing/empty/populated) — add to all. More importantly add mapping_version to browse JSON meta and the XLSX export provenance/header so a frozen export proves its policy; else narrow the claim.
3. #583 docs/tests: update roxygen (category-normalization.R:28 still says 2→Limited,1→Refuted); replace/remove the stale duplicate normalizer test (test-unit-endpoint-functions.R:277-296) rather than "no change".
4. Migration-manifest test plan also updates: test-unit-analysis-snapshot-migration.R:9, test-mcp-select-principal-projections.R:179, test-unit-core-views-manifest.R:13.
5. Pre-046 behavior: the JSON parser returns an empty list (not NULL) for absent values (service.R:401); meta must convert missing/empty generator_json to omitted/NULL generator + generator_hash=NULL; add a pre-column fixture test.
6. Deployment: #585 changes worker-executed builder code → restart worker AND worker-maintenance before any backfill; a forced cluster refresh mints a new snapshot_id → force-refresh phenotype_functional_correlations afterward (dependency gate).
7. Cheap-route claim: comparisons router already mounted via mount_endpoint (no extra mount). The cheap-route isolation guard covers only health/auth/statistics; don't claim it — the /crosswalk route is pure in-memory, no external-budget wrapper needed; add a comparison-route static test only if claiming coverage.

## P2
- Build crosswalk anchor URL from the configured Axios base (axios.ts:10), handle relative base; open in new tab with safe rel.
- Add an MSW handler/typed-client test for /api/comparisons/crosswalk.
- Represent special inputs explicitly (SFARI NA → JSON null; NDD GeneHub named fallback) instead of ambiguous display strings.

## Confirmed / resolved
- Two normalizer consumers only: comparisons-list.R:64 (browse) + comparisons_endpoints.R:131 (upset definitive_only). PanelApp change also affects per-source dedup (Moderate now outranks Limited).
- Similarity/snapshots/clustering/richness/db-prep do NOT call the normalizer; MCP deliberately exposes raw → out of scope is correct. db-prep only copies raw GEL_Status (no mapping to sync).
- Hash argument correct: payload hash excludes raw/partition_validation/reproducibility (builder.R:499); input hash limited to type/params/source_version/dependencies (builder.R:505). Manifest-only provenance changes no payload/input/cluster hash, no LLM reuse, no CLUSTER_LOGIC_VERSION bump.
