# Codex adversarial DIFF review (#573 Slice A) — reconciliation

Reviewer: `gpt-5.6-terra`, high reasoning, read-only, one command-line pass (relaunched after a stdin hang). Raw: `2026-07-18-573-slice-a-diff-codex-review.md` (findings printed twice — streaming artifact; identical). Verdict: **DO-NOT-SHIP** → all BLOCKER/HIGH + the valid MEDIUM + LOW reconciled below. Also folds the whole-branch review's M1 + A2.

| # | Sev | Finding | Resolution |
|---|-----|---------|------------|
| B1 | BLOCKER | Caller-supplied `layers` controls **policy** fields `has_reproducibility` + `files_prefix`. An Admin can send `has_reproducibility:false` → skips the hard-coherence + reproducibility gates → publishes an incoherent/non-reproducible release; and `files_prefix:"../../target"` → path traversal in `analysis_release_build_tar_gz` (`file.path(src_dir, path)` w/o containment) escaping the temp dir. | `layers` is now a **SELECTION**, not a redefinition: `analysis_snapshot_release_resolve_layers()` matches each requested entry by `analysis_type` against the authoritative `analysis_snapshot_release_layers()` registry and uses the REGISTRY's `params`/`files_prefix`/`has_reproducibility` (caller's policy fields ignored). Unknown type / duplicate → 400. Plus defense-in-depth: `analysis_release_build_tar_gz` (and file materialization) reject any path with `..`, leading `/`, or absolute components. Confirmed live: my e2e's 2-layer subset build still works (selection), a policy override no longer does. |
| H1 | HIGH | Public `GET /releases`/`/latest`/`/<id>` return the **raw head** → leak `created_by_user_id` + `last_error_message` (+ status/scope/bundle_bytes/etc.). Confirmed live. | Public getters project to the spec §8 allowlist via `analysis_release_public_head()`: `{release_id, release_version, title, created_at, published_at, source_data_version, manifest_sha256, bundle_sha256, license, file_count, total_bytes, layers, zenodo}`. Admin path (raw head) unchanged. |
| H2 | HIGH | Build asserts a `reproducibility_hash` is PRESENT but never that `sha256(decoded_bytes) == reproducibility_hash`. A corrupt/restored bundle (bytes B, stored hash H≠sha256(B)) publishes a release whose `reproducibility.json` violates the LOCKED equality. | Build decodes the raw bytes once, computes `analysis_release_sha256(repro_bytes)`, and asserts `== reproducibility_hash`; mismatch → `release_reproducibility_missing` (400). New test with a mismatched bundle. |
| H3 | HIGH | Idempotency race: a failed 5s lock acquisition proceeds UNLOCKED; two identical concurrent builds both see no release, both insert, one loses the PK → raw 500 instead of idempotent 200; genuine 64-bit collision not distinguished. | (a) On a real DBIConnection, a failed per-preset lock → `release_lock_unavailable` → service maps to **503 + Retry-After** (sources may be mid-refresh); test-env non-DBI skips lock as before. (b) The insert is wrapped: on a duplicate-key error re-read by `release_id` — if stored `content_digest` matches → idempotent `created=FALSE` (200); else `stop()` (500 anomaly). Race-safe regardless of the lock. |
| M1 | MEDIUM | `db_release_version`/`db_release_commit` never populated (NULL/NA) despite migration + design provenance contract. Confirmed the snapshot manifest carries them (`1.0.0`/`unknown`). | Extract `db_release_version`/`db_release_commit` from the pinned snapshot manifests (assert consistent, else take first non-NA), persist on the release head + `manifest.source.db_release{version,commit}`. |
| L1 | LOW | Public pagination unbounded: `limit=-1` → DB error; huge limit → returns every release. | `svc_release_list`/route clamp `limit` to `[1, 100]` and `offset` to `>= 0`. |
| WB-M1 | (whole-branch) | Minor | `analysis_snapshot_prune` hard-calls `analysis_release_referenced_snapshot_ids()` (not on the mirai pool). `exists()`-guard the call (mirai-pool parity), mirroring the existing lock-name guard. |
| WB-A2 | (whole-branch) | Minor | `analysis_release_content_digest` sort → `order(analysis_types, method = "radix")` for locale-invariant content identity (de-risks #574). |

Codex explicitly checked-OK: exact `(release_id,file_path)` PK file lookup; SQL-published filter; admin Administrator-gating; bound `unname()` params; migration manifest `045`/`43L`; single Content-Type on byte routes; pool/lock `on.exit` cleanup.

After the fixes land + green, **re-run Codex** to confirm SHIP (per the "Codex expands scope → re-run to ship" discipline).

## Codex RE-REVIEW (round 2): BLOCKER+3 HIGH+LOW confirmed resolved; 1 NEW HIGH + MEDIUM + LOW

| # | Sev | Finding | Resolution |
|---|-----|---------|------------|
| H4 | HIGH | Release "hard coherence" re-check calls `analysis_snapshot_assert_partition_coherent` WITHOUT `membership_members`/`validation_members`, so it never verifies MEMBER-SET content (only cluster-id set + channel + non-NA stability). Under `ANALYSIS_SNAPSHOT_REQUIRE_COHERENCE=false`, a snapshot whose stale served membership shares cluster-ids+channel with the validation but differs in MEMBERS can be frozen publicly — violating the design's "an incoherent snapshot can never be frozen into a release." `reference_members` is not persisted, so the release can't reconstruct it. | **Persist an additive coherence attestation at snapshot build** (in `partition_validation` → `validation_json`, excluded from `payload_hash` → NO cluster_hash churn / NO LLM regen / NO membership change): `analysis_snapshot_join_validated_clusters` threads `val$reference_members` into `val$partition` (the stored block). The RELEASE reads them as `validation_members`, reconstructs `membership_members` from the stored `cluster_members`, and calls the helper with `require_coherence=TRUE` → the full same-partition member-set proof. Legacy snapshots lacking the attestation → graceful degrade to the current channel+stability substitute + a WARNING (do NOT hard-reject old snapshots). Worker-executed builder change → worker restart + snapshot rebuild to take full effect (documented in the methodology-deploy runbook). |
| M2 | MEDIUM | `db_release_version`/`commit` conflict across layers silently takes the FIRST value → a release can claim `v1` while a layer is `v2`. | Assert consistency: if the distinct NON-EMPTY db_release values across layers disagree → reject the build (classed error → 400), matching the existing `source_data_version` consistency gate (snapshots with the same source_data_version should share a db_release). |
| L2 | LOW | Public pagination response echoes the UNCLAMPED request `limit`/`offset` (e.g. reports `limit:1000000, offset:-1` while serving ≤100 from offset 0). | Return the EFFECTIVE (clamped) `limit`/`offset` in the pagination response. |

(Codex also flagged a #574 `job-functional-submission-service.R` dedupe note — that is #574/Slice D scope, out of this PR; the file is unchanged by Slice A.)

## Codex RE-REVIEW (round 3): H4 core CONFIRMED correct; NO BLOCKER; Not SHIP on 1 HIGH + 2 MEDIUM + LOW

| # | Sev | Finding | Resolution |
|---|-----|---------|------------|
| CI1 | HIGH | `analysis-snapshot-repository.R` grew to 603 lines (>600, not baseline-allowlisted) → `make code-quality-audit` fails CI. | Extract the release-reference/prune decision into a cohesive helper (its own small file or the release repository), bringing analysis-snapshot-repository.R ≤ 600. |
| MC1 | MEDIUM | `.analysis_snapshot_string_to_hgnc_dict` dedups by STRING_id (first-wins). If a STRING protein maps to MULTIPLE hgnc records (`non_alt_loci_set` has no STRING_id uniqueness), the served cluster_members contain all joined hgnc ids but the attestation only the first → a COHERENT functional snapshot is FALSE-REJECTED at release. | Build the dict as STRING_id → SET of hgnc_ids (preserve ALL mappings); expand each reference STRING_id to all its hgnc_ids so the mapped reference set matches the served set. One-to-many mapping test. |
| MC2 | MEDIUM | The same-partition proof checks only the INTERSECTION of attestation + served cluster ids; a non-empty but PARTIAL/malformed attestation can omit a mismatching cluster and pass the hard check. | When an attestation is present, require its key set to EQUAL the served/validated cluster set; reject partial/malformed as incoherent (do NOT treat as legacy-absent). Test with a partial attestation. |
| WS1 | LOW | Trailing whitespace in the committed review-note MD files → `git diff --check 67cf6003..HEAD` fails. | Strip trailing whitespace from the flagged review files. |

Codex round-3 CONFIRMED: H4 hard-forces coherence for complete one-to-one attestations; `partition_validation` still excluded from `payload_hash` (builder:502); db_release conflict gate + clamped pagination echo resolved; layer-selection, public allowlist, reproducibility-hash, locking, idempotency all intact.
