1. **P1 — “The union is the only cause” is not established.**  
   The union is the direct cause of the reported normal-path bug, but the safety argument is false. `getEntity()` and `loadReviewInfo()` swallow failures ([useReviewData.ts](/home/bernt-popp/development/sysndd/app/src/views/curate/composables/useReviewData.ts:307), [useReviewData.ts](/home/bernt-popp/development/sysndd/app/src/views/curate/composables/useReviewData.ts:326)), after which the modal still opens ([Review.vue](/home/bernt-popp/development/sysndd/app/src/views/review/Review.vue:418)). Submission then chooses POST versus PUT from potentially missing/stale `review_info.review_id` ([Review.vue](/home/bernt-popp/development/sysndd/app/src/views/review/Review.vue:461)), not from the review successfully loaded into `useReviewForm`.  
   **Correction:** narrow the root-cause claim; make review-metadata failure prevent editing, or derive update mode from the authoritative loaded form review ID. Add a `Review.spec.ts` failure-path test.

2. **P1 — The spec’s “empty form means curator removed everything” invariant is contradicted by the real read path.**  
   The form recognizes only exact `gene_review` and `additional_references` values ([useReviewForm.ts](/home/bernt-popp/development/sysndd/app/src/views/curate/composables/useReviewForm.ts:322)). Any other stored relation type is silently omitted, including the fixture’s current `PMID` row. The schema permits arbitrary strings. Therefore an empty `formData.publications` can represent filtered-out database data, not a curator decision.  
   **Correction:** validate/fail closed on unknown relation types or enforce/migrate a constrained vocabulary. Do not present the frontend binding as a sufficient invariant.

3. **P1 — The feared R positional-collapse defect is not present, but the proposed tests do not prove that.**  
   The endpoint does not compact the two-element literature list and then bind it. It separately converts each named element and passes two positional data frames to `bind_rows(..., .id=...)` ([review_endpoints.R](/home/bernt-popp/development/sysndd/api/endpoints/review_endpoints.R:225)). A zero-row first tibble is retained, so nonempty `gene_review` rows still receive id `"2"` and map correctly ([review_endpoints.R](/home/bernt-popp/development/sysndd/api/endpoints/review_endpoints.R:232)). The service then performs an exact replacement, including the explicit zero-row case ([review-write-service.R](/home/bernt-popp/development/sysndd/api/services/review-write-service.R:468), [publication-repository.R](/home/bernt-popp/development/sysndd/api/functions/publication-repository.R:267)).  
   **Correction:** add API endpoint tests for all four combinations: both populated, only additional references, only GeneReviews, and both empty. Frontend composable tests cannot catch a future `.id` regression.

4. **P1 — The fixture change is incomplete and destabilizes existing E2E behavior.**  
   Changing the join relation from `PMID` to `additional_references` is correct, but the fixture also stores the publication key as bare `12345678` ([playwright_e2e_baseline.sql](/home/bernt-popp/development/sysndd/db/fixtures/playwright_e2e_baseline.sql:265), [playwright_e2e_baseline.sql](/home/bernt-popp/development/sysndd/db/fixtures/playwright_e2e_baseline.sql:619)). The frontend requires canonical `PMID:<digits>` tags ([useReviewForm.ts](/home/bernt-popp/development/sysndd/app/src/views/curate/composables/useReviewForm.ts:77)). Making the row visible without fixing the key seeds an invalid UI value. It also invalidates the provenance E2E’s explicit assumption that this row is filtered out ([curate.variation-provenance.spec.ts](/home/bernt-popp/development/sysndd/app/tests/e2e/curate.variation-provenance.spec.ts:188)) and causes its saves to enter live GeneReviews classification ([publication-write-preparation.R](/home/bernt-popp/development/sysndd/api/functions/publication-write-preparation.R:39)).  
   **Correction:** seed `PMID:12345678` in both tables, then update/isolate the existing provenance test and its external-call assumptions. Keep `publication.publication_type='PMID'`; only the join’s relation type is wrong.

5. **P1 — The proposed E2E is not implementable as described.**  
   `reseedBaseline`, `reviewSaveWorks`, `openReviewModal`, `saveReview`, and `curatorToken` are file-local, not exported ([curate.variation-provenance.spec.ts](/home/bernt-popp/development/sysndd/app/tests/e2e/curate.variation-provenance.spec.ts:60), [curate.variation-provenance.spec.ts](/home/bernt-popp/development/sysndd/app/tests/e2e/curate.variation-provenance.spec.ts:199)). Browser login does not authenticate Playwright’s separate `request` fixture, while the publications endpoint requires Reviewer authorization ([review_endpoints.R](/home/bernt-popp/development/sysndd/api/endpoints/review_endpoints.R:437)). `getByLabel("Publications")` will also fail: the label targets `review-publications-select`, but the widget uses `review-literature-select` ([ReviewFormFields.vue](/home/bernt-popp/development/sysndd/app/src/views/curate/components/ReviewFormFields.vue:189), [ReviewFormFields.vue](/home/bernt-popp/development/sysndd/app/src/views/curate/components/ReviewFormFields.vue:207)). Finally, `reviewSaveWorks` probes empty literature, so it does not exercise the external classification triggered by the newly visible fixture publication; its `test.skip` path can eliminate all protection.  
   **Correction:** extract shared authenticated helpers, add a stable tag/remove selector, explicitly attach the bearer token to API assertions, and keep CI unit/API coverage authoritative. Reseed in `afterEach`/`finally`, not merely at the successful end.

6. **P1 — The claimed audit-log mitigation does not exist on the real write path.**  
   The publication-decrease warning runs only when `publication_replace_for_review()` owns its connection ([publication-repository.R](/home/bernt-popp/development/sysndd/api/functions/publication-repository.R:225)). The review service always passes `txn_conn` ([review-write-service.R](/home/bernt-popp/development/sysndd/api/services/review-write-service.R:472)), bypassing that block.  
   **Correction:** remove the audit-trail claim or move transaction-compatible counting/logging into the service/repository path actually used.

7. **P1 — The frontend test plan conflicts with the proposed implementation.**  
   The existing deduplication comes from `new Set([...original, ...current])` ([useReviewForm.ts](/home/bernt-popp/development/sysndd/app/src/views/curate/composables/useReviewForm.ts:397)). Sending current arrays “directly” removes that behavior, while the proposed matrix still expects deduplication. The existing “reactivity cleared” test also explicitly enshrines the bug ([useReviewForm.spec.ts](/home/bernt-popp/development/sysndd/app/src/views/curate/composables/__tests__/useReviewForm.spec.ts:147)).  
   **Correction:** deduplicate the current selection only, e.g. conceptually `Set(formData.publications)`, and replace the obsolete test. Include the exact `publications=[]`, `genereviews=[...]` payload case.

8. **P1 — The dependency integration and verification plan is incomplete.**  
   Removing `@types/markdown-it` for markdown-it 15 is correct because v15 ships its own types. However, v15 disables fuzzy bare-domain links by default; `linkify: true` alone no longer preserves the renderer’s previous “auto-link URLs” behavior ([renderer](/home/bernt-popp/development/sysndd/app/src/composables/useMarkdownRenderer.ts:11), [official v15 migration guide](https://github.com/markdown-it/markdown-it/blob/master/docs/migration/migration_v15.md)). The spec must explicitly preserve fuzzy links with `md.linkify.set({ fuzzyLink: true })` or document the intentional behavior change. Also, `make ci-local` does not run frontend Vitest or the production bundle build ([Makefile](/home/bernt-popp/development/sysndd/Makefile:231)); CI runs those separately ([ci.yml](/home/bernt-popp/development/sysndd/.github/workflows/ci.yml:449)).  
   **Correction:** make the fuzzy-link policy explicit, run `npm run test:unit` and `npm run build:bundle-budget`, and preferably keep the four Dependabot updates separate from the bug fix.

9. **P1 — The durable documentation contract is omitted.**  
   Repository instructions require behavior changes to update durable documentation ([AGENTS.md](/home/bernt-popp/development/sysndd/AGENTS.md:319)). This is precisely a stack-specific replacement-set trap: review publication arrays are authoritative and must not be unioned with loaded originals.  
   **Correction:** add an `AGENTS.md` gotcha. A changelog entry alone is insufficient contributor guidance.

10. **P2 — The draft-safety reasoning is inaccurate.**  
    Review forms use the constant `review-form-new` key because no entity ID is supplied ([Review.vue](/home/bernt-popp/development/sysndd/app/src/views/review/Review.vue:254), [useReviewForm.ts](/home/bernt-popp/development/sysndd/app/src/views/curate/composables/useReviewForm.ts:206)). `clearDraft()` does not cancel an already queued debounce ([useFormDraft.ts](/home/bernt-popp/development/sysndd/app/src/composables/useFormDraft.ts:124), [useFormDraft.ts](/home/bernt-popp/development/sysndd/app/src/composables/useFormDraft.ts:162)). Draft bleed is currently prevented because Review never calls `restoreFromDraft`, not because clearing is race-proof.  
    **Correction:** state the real invariant or disable draft persistence for this modal; cancel pending writes when clearing.

11. **P2 — Several file/line claims and the affected-file inventory are stale.**  
    The four-read `Promise.all` is at [useReviewForm.ts](/home/bernt-popp/development/sysndd/app/src/views/curate/composables/useReviewForm.ts:290), not 296–301; modal display is [Review.vue](/home/bernt-popp/development/sysndd/app/src/views/review/Review.vue:418), not 415. The inventory also omits `AGENTS.md`, renderer tests/source if fuzzy behavior is preserved, and the Compose/package files introduced by the bundled dependency PRs.  
    **Correction:** regenerate all anchors and list every intended modification before implementation.

**DO-NOT-SHIP the spec as written.**

Codex session ID: 01a03888-e078-7480-8875-264f46ba7ec0
Resume in Codex: codex resume 01a03888-e078-7480-8875-264f46ba7ec0
