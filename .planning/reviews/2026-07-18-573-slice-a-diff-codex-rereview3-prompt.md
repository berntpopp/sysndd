# Codex re-review (round 3) — #573 Slice A, verify the coherence-attestation fix

Prior rounds: your BLOCKER + 3 HIGH + LOW were fixed and confirmed resolved; round 2 you raised a NEW HIGH (release hard-coherence never verified MEMBER-SET content) + a MEDIUM (db_release conflict silently takes first) + a LOW (pagination echoes unclamped values). Fix commits `338aa92c` (H4+M2) and `75c62c46` (L2) were then applied. Re-review `git diff 67cf6003..HEAD` (whole Slice A), and CONFIRM each round-2 finding is resolved AND adversarially scrutinize the new coherence-attestation code for correctness/regression.

Round-2 fixes to verify:
- HIGH (member-set coherence): the snapshot builder now persists the validator's reference member sets additively into `validation_json` (via `analysis_snapshot_join_validated_clusters` -> `val$partition`), and the release reconstructs served membership from `cluster_members` and runs the full same-partition proof (`analysis_snapshot_assert_partition_coherent(..., require_coherence=TRUE)`). Because the stored `cluster_members` keeps `hgnc_id`/`entity_id` (not `STRING_id`), the reference sets are mapped into the STORED id space: phenotype is already `entity_id`; functional `STRING_id` -> `hgnc_id` via a partition-independent dict built from the served membership `identifiers` (`.analysis_snapshot_string_to_hgnc_dict` / `analysis_snapshot_reference_members_store_space` in analysis-snapshot-coherence.R), unmapped ids kept verbatim (fail-closed). Legacy snapshots lacking the attestation degrade to the channel+stability check + a warning (not hard-rejected).
  SCRUTINIZE ESPECIALLY: (1) can this FALSELY REJECT a genuinely COHERENT functional snapshot (an availability bug) — e.g. if the served `identifiers` doesn't cover every STRING_id in the reference, or the STRING->hgnc mapping is not 1:1, or ordering/dedup drops a member? (2) can it FALSELY ACCEPT an incoherent one (mask a real mismatch) — e.g. via the verbatim-keep, empty-set, or NA handling? (3) is the additive `validation_json` write truly excluded from `payload_hash` (no cluster_hash churn / no membership change)? (4) any place the attestation could be attacker-influenced or absent-by-default in a way that downgrades the gate silently?
- MEDIUM (db_release): disagreeing non-empty `db_release_version`/`commit` across layers now -> `release_source_version_mismatch` (400). Verify empty/NA is not treated as a conflict and the gate is correct.
- LOW (pagination): response now echoes clamped `limit`([1,100])/`offset`(>=0). Verify.

Also re-confirm the earlier resolved findings did not regress (layer selection, public-head allowlist, reproducibility-hash assertion, lock/idempotency). Group findings BLOCKER/HIGH/MEDIUM/LOW with file:line + failure scenario + fix. If all resolved and no new BLOCKER/HIGH, end with SHIP. Read-only; do not modify files.
