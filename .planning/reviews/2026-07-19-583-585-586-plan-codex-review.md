# Codex 5.6-terra (high) — PLAN review: #583/#585/#586

Model gpt-5.6-terra, effort high, read-only. Verdict: do NOT execute unchanged;
several literal compile/runtime failures + false-green tests. Codex session
019f7c2c-a407-73e3-812e-76fdf050adf6.

## P0 (blocking) — all confirmed against real code
1. C1 client snippet won't compile: repo `apiClient.get<T>()` already returns data (client.ts:138); no `api`/`{data}`. Use `return apiClient.get<ComparisonsCrosswalk>('/api/comparisons/crosswalk')`.
2. C2 imports nonexistent `API_URL` from `@/plugins/axios` (only sets `axios.defaults.baseURL = VITE_BASE_URL`). Use `import axios from 'axios'` + `axios.defaults.baseURL`.
3. BPopover `triggers="click"` unsupported in installed BVN (boolean `click`/`focus`/`hover`); planned component stays hover/focus + no Escape. Use boolean props + explicit visibility model, Escape-to-close + focus restore, tested.
4. B2 version resolution returns "unknown" in the worker: `start_async_worker.R` doesn't init `version_json`; `get_api_dir()` is a test helper. Make version resolvable in worker (init the global in worker bootstrap or a worker-safe path); test the no-global/non-api-cwd path.
5. B2 test won't load deps: must source the generator helper + cache-fingerprint (CLUSTER_LOGIC_VERSION) + the `%||%` source; extend the worker-preload order test.
6. B3 records only `normalized$params` (public preset), not applied algorithm settings (resolution/seed/thresholds; phenotype ncp/kk/consol). Capture actual applied clustering params. Fix invalid test type `network_edges` -> `gene_network_edges`.
7. B3 file-size: `analysis-snapshot-builder.R` is exactly 600 lines; adding code fails `make code-quality-audit`. Extract a cohesive block first (e.g. source_versions/db_release resolution) to net <=600.
8. B5 reads `meta.snapshot.generator` which release assembly never builds (works from `snapshot$manifest`). Parse `manifest$generator_json` directly into `source.snapshots[]`; keep out of content_digest.
9. C2 hard-codes tier/PanelApp text (fetches only mapping_version) -> contradicts API-sourced no-drift; test passes even if server content changes. Render tiers/rules/notes from the crosswalk payload; test with distinctive mocked content.

## P1 (should fix)
1. A2 crosswalk omits `omim_ndd` + `orphanet_id` passthrough (normalizer passes them through too). Add + skip passthrough in guard.
2. A3 `/crosswalk` serializer `list(na="string")` renders SFARI NA as "NA" not JSON null. Use `na="null"`; test via serializer/route.
3. A3 test is false-green (only calls the function). Add endpoint-level test: `/crosswalk` route, `generate_comparisons_list()$meta$mapping_version`, XLSX meta sheet (data-helpers.R:147 writes `data_object$meta`).
4. B3 hash test tautological (removes generator both sides). Exercise builder manifest seam / independently-computed hashes across generator values.
5. B5 command runs a unit test that never builds releases; use the integration harness (test-integration-analysis-snapshot-release-build.R).
6. Migration: add apply/reapply idempotency test; update stale message range `000-044`->`000-045` in verify-mcp-select-principal-live.R:198.
7. C2 test stubs can't prove interaction; use a real/behaviorful popover; test activate -> Tab reaches link -> Escape closes + restores focus.

## P2
- Document non-tier fillers (not applicable / not listed) in crosswalk response.
- C3 assert placement after the Overlap heading, not merely presence.

## Correct as written (Codex-confirmed)
- PanelApp ordinal (3->Definitive,2->Moderate,1->Limited) correct; A2 guard logic sound once all passthrough sources represented.
- Migration 046 idempotent PREPARE/EXECUTE follows repo pattern; 046 / count 44 correct.
- B3 insertion point has normalized$params/analysis_type in scope; provenance after hash calc doesn't touch payload/input/cluster hash or need CLUSTER_LOGIC_VERSION bump.
- B4 `service_analysis_snapshot_meta(list(manifest=row))` shape correct; digest available.
- content_digest already selects only scientific identity fields (excludes generator).
- A/B/C file-disjoint / parallel-safe; C depends on A's contract, not a merge conflict.
