# Codex adversarial review (#574) — reconciliation

Reviewer: `gpt-5.6-terra`, high reasoning, read-only, one command-line pass, against the committed `…category-clustering-universes-574-design.md` + `…-plan.md`. All 1 BLOCKER / 3 HIGH / 3 MEDIUM findings resolved in the design + plan.

| # | Sev | Finding | Resolution |
|---|-----|---------|------------|
| 1 | BLOCKER | D1 fixture resolved `["Definitive"]` to a single gene, but the resolver rejects `< 2` genes → the test can't pass (and D3 reuses it). | Fixture seeds **two** Definitive NDD genes (`HGNC:1`, `HGNC:5`); `["Definitive"] → {HGNC:1,HGNC:5}`; `["Moderate"]` (1 gene) and `["Refuted"]` (0) now exercise the too-small 400. Plan D1/D3. |
| 2 | HIGH | Empty/whitespace `category_filter` normalized to `NULL` → treated as the all-NDD default instead of 400. | `clustering_normalize_category_filter` now returns `NULL` **only** for an absent arg and `character(0)` for supplied-but-empty; presence decided by `!is.null(req$argsBody$category_filter)`; the resolver 400s `character(0)`. Design §4, Plan D1. |
| 3 | HIGH | `string_weight_channel` is only known **after** clustering (exp+db can fall back to `combined_score`), so the immutable submit identity can't record the effective method. | Split into an **intended** fingerprint (`analysis_string_cache_fingerprint()`) in the identity/payload and an **effective** fingerprint (`attr(clusters,"weight_channel")`) in the result meta (set by the cache-hit path and the worker handler). Fail closed, no `NA`. Design §6, Plan D2/D3. |
| 4 | HIGH | `analysis_snapshot_source_data_version()` runs global counts/joins and was called **before** capacity/dup admission on an unauthenticated path → distributed flood forces expensive scans. | `clustering_cached_source_data_version()` (bounded cache), fetched **only when building a payload** (post-admission), **fail-closed 503** on error. Design §6/§9, Plan D2 + Global Constraints. |
| 5 | MEDIUM | The "allowed set" was passed as `stop_for_bad_request(detail=…)`, but the handler serializes only `conditionMessage(err)` → clients never see it. | Allowed active-category set is now in the error **message** string. Design §4/§9, Plan D1 (code + test asserts the message contains the allowed values). |
| 6 | MEDIUM | The "identical submit → 409" claim is false: preflight is a no-op, `create_job` discards the duplicate signal (→202), warm-cache repeats each create a completed row (NULL `active_request_hash`). | Reframed: #574 makes the `request_hash` **selector-aware**; dedup stays active-only/best-effort with **no HTTP 409**. Tests assert `request_hash` distinctness/equality, not a 409. Design §7/§8/§9, Plan D2/D3. |
| 7 | MEDIUM | The `{kind, category_filter}` selector object wasn't actually persisted (only a char vector) → explicit-all-NDD vs no-arg indistinguishable in provenance. | The full **selector object** `{kind, category_filter}` is persisted in provenance; `category_filter` is added to the dedup payload **only** for category selectors, so explicit/no-arg contracts (and their `request_hash`) are byte-identical to today. Design §6/§7, Plan D2. |

No finding required phenotype-clustering symmetry, a public category GET, a snapshot preset, or fixing the pre-existing preflight/DB hash-scope inconsistency — scope held to #574.
