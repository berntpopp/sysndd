---
phase: 16-ui-ux-modernization
plan: 07
type: execute
wave: 4
depends_on: ["16-03"]
files_modified:
  - app/src/assets/scss/components/_responsive.scss
  - app/src/assets/scss/custom.scss
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Tables transform to card view on mobile (< 768px)"
    - "Navigation is usable on mobile devices"
    - "All breakpoints tested and responsive"
    - "Data labels visible in mobile card view"
  artifacts:
    - path: "app/src/assets/scss/components/_responsive.scss"
      provides: "Mobile responsive refinements"
      contains: "@media"
  key_links:
    - from: "app/src/assets/scss/components/_responsive.scss"
      to: "_tables.scss"
      via: "Table card transformation"
      pattern: "table.*card|stacked"
---

<objective>
Implement mobile responsive refinements including table-to-card transformation.

Purpose: Per FR-07.9: "Improve mobile responsive behavior (table -> card on small screens)". Data-heavy tables are unusable on mobile without transformation. This plan adds CSS for responsive table behavior.

Output: Responsive SCSS partial with table-to-card transformation and mobile navigation improvements.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-ui-ux-modernization/16-CONTEXT.md
@.planning/phases/16-ui-ux-modernization/16-RESEARCH.md
@.planning/phases/16-ui-ux-modernization/16-03-SUMMARY.md
@app/src/components/tables/TablesGenes.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create responsive SCSS with table-to-card transformation</name>
  <files>
    app/src/assets/scss/components/_responsive.scss
    app/src/assets/scss/custom.scss
  </files>
  <action>
    Create `components/_responsive.scss` with mobile responsive styles:

    1. Table-to-card transformation (< 768px):
       - Use Bootstrap's stacked="md" attribute on BTable (already in place)
       - Enhance the stacked appearance with card-like styling:
         - Each row becomes a card with shadow and border-radius
         - margin-bottom: 1rem between rows
         - padding: 0.75rem
         - box-shadow: var(--shadow-xs)

    2. Mobile table row styling:
       - Hide table headers on mobile (Bootstrap handles this)
       - Show data labels using [data-label] attribute on td
       - td::before { content: attr(data-label); font-weight: 600; }
       - Flexbox layout for label-value pairs

    3. Priority columns:
       - Use Bootstrap's .d-none.d-md-table-cell for less important columns
       - Hide secondary columns on mobile

    4. Container adjustments:
       - Reduce padding on mobile: .container-fluid padding on xs
       - Card body padding reduction on mobile

    5. Update custom.scss to import components/_responsive.scss

    Note: Bootstrap-Vue-Next BTable with stacked="md" already handles basic stacking. This enhances the visual presentation.
  </action>
  <verify>
    - app/src/assets/scss/components/_responsive.scss exists
    - Contains @media (max-width: 767px) or similar
    - Build succeeds: `npm run build --prefix app`
  </verify>
  <done>
    Table-to-card transformation implemented for mobile devices
  </done>
</task>

<task type="auto">
  <name>Task 2: Improve mobile navigation and overall responsiveness</name>
  <files>app/src/assets/scss/components/_responsive.scss</files>
  <action>
    Add mobile navigation improvements to _responsive.scss:

    1. Navigation bar on mobile:
       - Ensure hamburger menu is accessible
       - Proper touch targets (min 44x44px)
       - Adequate spacing between nav items

    2. Search and filter controls on mobile:
       - Full-width search input on mobile
       - Stack filter controls vertically
       - Pagination controls responsive

    3. Card and button sizing:
       - Full-width buttons on mobile where appropriate
       - Card margins adjusted for small screens

    4. Typography adjustments:
       - Slightly smaller headings on mobile
       - Maintain readable line length

    5. Touch-friendly interactions:
       - Larger tap targets for interactive elements
       - Adequate spacing between clickable items

    6. Test breakpoints:
       - 320px (small phone)
       - 375px (standard phone)
       - 768px (tablet)
       - 1024px+ (desktop)
  </action>
  <verify>
    - _responsive.scss contains mobile navigation styles
    - Contains touch target sizing
    - Build succeeds
  </verify>
  <done>
    Mobile navigation and overall responsiveness improved
  </done>
</task>

</tasks>

<verification>
```bash
# Build succeeds
npm run build --prefix app

# Check responsive styles in output
grep -r "@media.*max-width\|stacked" app/dist/assets/*.css | head -5

# Start dev and test responsiveness
npm run dev --prefix app &
sleep 3
echo "Check http://localhost:5173/Genes at different viewport widths"
echo "Use browser DevTools to test 320px, 375px, 768px"
kill %1
```
</verification>

<success_criteria>
- [ ] Table-to-card transformation works at < 768px
- [ ] Data labels visible in stacked mobile view
- [ ] Navigation usable on mobile
- [ ] Touch targets are adequate size
- [ ] All breakpoints tested (320px, 375px, 768px)
- [ ] Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/16-ui-ux-modernization/16-07-SUMMARY.md`
</output>
