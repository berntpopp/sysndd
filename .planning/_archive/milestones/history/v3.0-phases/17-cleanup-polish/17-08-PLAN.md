---
phase: 17-cleanup-polish
plan: 08
type: execute
wave: 6
depends_on: ["17-06", "17-07"]
files_modified:
  - README.md
  - documentation/*.md
  - CHANGELOG.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "README.md updated with Vue 3 + TypeScript + Vite stack"
    - "CHANGELOG.md created with all v3 migration changes"
    - "Developer documentation updated in documentation/ folder"
    - "Breaking changes clearly documented"
  artifacts:
    - path: "README.md"
      provides: "Updated quick start and stack info"
      contains: "Vite"
    - path: "CHANGELOG.md"
      provides: "Version history with breaking changes"
      contains: "## [1.0.0]"
    - path: "documentation/DEVELOPMENT.md"
      provides: "Developer setup guide"
      contains: "npm run dev"
  key_links:
    - from: "README.md"
      to: "documentation/"
      via: "documentation link"
      pattern: "documentation/"
---

<objective>
Update all documentation for the Vue 3 + TypeScript + Vite stack

Purpose: Ensure developers can onboard quickly with accurate documentation
Output: Updated README, comprehensive CHANGELOG, developer documentation
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-cleanup-polish/17-CONTEXT.md
@.planning/phases/17-cleanup-polish/17-RESEARCH.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md with new stack information</name>
  <files>README.md</files>
  <action>
Read current README.md and update the "Quick start" section.

Keep existing content (logo, description, table of contents, creators, funding, etc.).

Update Quick start section to reflect new stack:

```markdown
## Quick start

### Prerequisites

- Docker and Docker Compose
- Node.js 24 LTS (for local development)

### Docker Deployment

```bash
bash deployment.sh "<config.tar.gz>"
```

### Local Development

1. Clone the repository
2. Set up environment:
```bash
cp app/.env.example app/.env
# Edit .env with your API URL
```

3. Install dependencies:
```bash
cd app && npm install
```

4. Start development server:
```bash
npm run dev
```

The app runs at http://localhost:5173

### Build for Production

```bash
cd app && npm run build:production
```

### Run Tests

```bash
cd app && npm run test:unit
```
```

Add a new "Tech Stack" section after Quick start:

```markdown
## Tech Stack

**Frontend:**
- Vue 3.5 with Composition API
- TypeScript (relaxed strict mode)
- Bootstrap-Vue-Next 0.42
- Vite 7 build tooling
- Vitest for testing
- Pinia for state management

**Backend:**
- R/Plumber API
- MySQL 8.0

**Infrastructure:**
- Docker with Traefik reverse proxy
- Multi-stage builds with BuildKit
```

Keep all existing sections below (Documentation, Contributing, Bugs, Creators, etc.).
  </action>
  <verify>
```bash
grep -A5 "Quick start" README.md
grep "Vite" README.md
```
README updated with new stack info.
  </verify>
  <done>README.md updated with Vue 3 + Vite stack and new commands</done>
</task>

<task type="auto">
  <name>Task 2: Create CHANGELOG.md</name>
  <files>CHANGELOG.md</files>
  <action>
Create CHANGELOG.md in project root following Keep a Changelog format:

```markdown
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

## [1.0.0] - 2026-01-XX

This release completes the v3 Frontend Modernization milestone, migrating the SysNDD frontend from Vue 2 to Vue 3 with TypeScript support.

### Added
- Vue 3 Composition API (`<script setup>` syntax)
- TypeScript support with relaxed strict mode
- Vite 7 build system (replaces Vue CLI + Webpack)
- Bootstrap-Vue-Next 0.42 (replaces Bootstrap-Vue)
- Pinia state management
- Vitest + Vue Test Utils for frontend testing (144+ tests)
- MSW for API mocking in tests
- CSS design tokens system for theming
- Loading skeleton components with shimmer animation
- Empty state components
- Enhanced table styling with zebra striping and sort indicators
- Form validation with vee-validate 4
- Composables pattern replacing mixins

### Changed
- **BREAKING:** Minimum Node.js version is now 24 LTS
- **BREAKING:** Development server runs on port 5173 (was 8080)
- **BREAKING:** Use `npm run dev` instead of `npm run serve`
- **BREAKING:** Bootstrap upgraded from v4 to v5
- SCSS uses `@use` syntax instead of `@import`
- Toast notifications: danger toasts require manual dismissal (medical safety)
- Environment variables use VITE_* prefix

### Removed
- **BREAKING:** @vue/compat compatibility layer
- **BREAKING:** global-components.js (components now imported explicitly)
- Vue CLI dependencies (@vue/cli-service, vue-cli-plugin-*)
- Webpack dependencies
- All Vue 2 mixins (migrated to composables)
- Bootstrap-Vue (replaced by Bootstrap-Vue-Next)

### Fixed
- Bundle size optimized for <2MB gzipped target
- ESM compatibility issues with Node.js 18+
- TypeScript compilation errors

### Security
- npm audit vulnerabilities fixed
- Removed deprecated packages

## Migration Guide

### For Developers

1. **Node.js:** Upgrade to Node 24 LTS or later
2. **Install:** Run `npm install` to update dependencies
3. **Development:** Use `npm run dev` (not `npm run serve`)
4. **Build:** Use `npm run build:production` for production builds
5. **Tests:** Run `npm run test:unit`

### Breaking Changes Details

**global-components.js removed:**
Components are no longer auto-registered globally. Import explicitly:

\`\`\`vue
<!-- Before (implicit) -->
<template>
  <TablesGenes />
</template>

<!-- After (explicit) -->
<script setup lang="ts">
import TablesGenes from '@/components/tables/TablesGenes.vue';
</script>
<template>
  <TablesGenes />
</template>
\`\`\`

**Bootstrap 4 to 5:**
Some class names changed. See [Bootstrap 5 Migration Guide](https://getbootstrap.com/docs/5.3/migration/).
- `ml-*` -> `ms-*` (margin-left -> margin-start)
- `mr-*` -> `me-*` (margin-right -> margin-end)
- `pl-*` -> `ps-*` (padding-left -> padding-start)
- `pr-*` -> `pe-*` (padding-right -> padding-end)

[Unreleased]: https://github.com/berntpopp/sysndd/compare/v1.0.0...HEAD
[1.0.0]: https://github.com/berntpopp/sysndd/releases/tag/v1.0.0
```

Note: Update the date (2026-01-XX) when the release is finalized.
  </action>
  <verify>
```bash
cat CHANGELOG.md | head -50
```
CHANGELOG.md created with proper format.
  </verify>
  <done>CHANGELOG.md created with complete v3 migration history and breaking changes</done>
</task>

<task type="auto">
  <name>Task 3: Create/update developer documentation</name>
  <files>documentation/DEVELOPMENT.md</files>
  <action>
Create or update documentation/DEVELOPMENT.md for developer onboarding:

```markdown
# SysNDD Development Guide

This guide covers local development setup and common tasks.

## Prerequisites

- Node.js 24 LTS or later
- Docker and Docker Compose (for database and API)
- Git

## Quick Start

### 1. Clone and Setup

\`\`\`bash
git clone https://github.com/berntpopp/sysndd.git
cd sysndd
cp app/.env.example app/.env
\`\`\`

Edit `app/.env` to set your API URL (use Docker API or local API).

### 2. Install Dependencies

\`\`\`bash
cd app
npm install
\`\`\`

### 3. Start Development Server

\`\`\`bash
npm run dev
\`\`\`

Open http://localhost:5173 in your browser.

## Common Tasks

### Run Tests
\`\`\`bash
npm run test:unit      # Run tests once
npm run test:watch     # Watch mode
npm run test:coverage  # With coverage report
\`\`\`

### Lint and Format
\`\`\`bash
npm run lint           # Check for issues
npm run lint:fix       # Auto-fix issues
npm run format         # Format with Prettier
\`\`\`

### Build for Production
\`\`\`bash
npm run build:production
npm run preview        # Preview production build
\`\`\`

### Type Check
\`\`\`bash
npm run type-check
\`\`\`

## Project Structure

\`\`\`
app/
├── src/
│   ├── assets/          # SCSS styles
│   ├── components/      # Vue components
│   │   ├── analyses/    # D3/visualization components
│   │   ├── small/       # Small UI components
│   │   ├── tables/      # Table components
│   │   └── ui/          # Loading/Empty state components
│   ├── composables/     # Vue 3 composables
│   ├── router/          # Vue Router configuration
│   ├── services/        # API service
│   ├── stores/          # Pinia stores
│   ├── types/           # TypeScript types
│   └── views/           # Page components
├── public/              # Static assets
├── tests/               # Test utilities
├── vite.config.ts       # Vite configuration
└── tsconfig.json        # TypeScript configuration
\`\`\`

## Code Patterns

### Components
Use Composition API with `<script setup lang="ts">`:

\`\`\`vue
<script setup lang="ts">
import { ref, computed } from 'vue';
import type { Gene } from '@/types';

const props = defineProps<{
  gene: Gene;
}>();

const count = ref(0);
const doubled = computed(() => count.value * 2);
</script>

<template>
  <div>{{ gene.symbol }}: {{ doubled }}</div>
</template>
\`\`\`

### Composables
Use composables for reusable logic:

\`\`\`typescript
// src/composables/useGeneData.ts
import { ref, onMounted } from 'vue';

export function useGeneData(geneId: string) {
  const data = ref(null);
  const loading = ref(true);

  onMounted(async () => {
    data.value = await fetchGene(geneId);
    loading.value = false;
  });

  return { data, loading };
}
\`\`\`

### API Calls
Use the API service:

\`\`\`typescript
import ApiService from '@/services/api-service';

const response = await ApiService.getGene(geneId);
\`\`\`

## Docker Development

Start full stack with Docker:

\`\`\`bash
make docker-up
\`\`\`

This starts:
- Frontend at http://localhost:5173
- API at http://localhost:7778
- Database at localhost:3306

## Debugging

### VS Code
The project includes VS Code settings for debugging.

### Vue DevTools
Install [Vue DevTools](https://devtools.vuejs.org/) browser extension.

### Network Issues
Check browser DevTools Network tab for failed API calls.
Verify .env has correct API_BASE_URL.

## Testing

### Component Tests
\`\`\`typescript
import { mount } from '@vue/test-utils';
import MyComponent from '@/components/MyComponent.vue';

test('renders correctly', () => {
  const wrapper = mount(MyComponent, {
    props: { title: 'Test' }
  });
  expect(wrapper.text()).toContain('Test');
});
\`\`\`

### Composable Tests
\`\`\`typescript
import { useText } from '@/composables/useText';

test('truncates text', () => {
  const { truncate } = useText();
  expect(truncate('Hello World', 5)).toBe('Hello...');
});
\`\`\`

## Need Help?

- [GitHub Discussions](https://github.com/berntpopp/sysndd/discussions)
- [Issue Tracker](https://github.com/berntpopp/sysndd/issues)
```
  </action>
  <verify>
```bash
cat documentation/DEVELOPMENT.md | head -50
```
Development documentation created.
  </verify>
  <done>Developer documentation created/updated in documentation/ folder</done>
</task>

</tasks>

<verification>
- [ ] README.md updated with new stack and commands
- [ ] CHANGELOG.md created with Keep a Changelog format
- [ ] Breaking changes clearly documented
- [ ] documentation/DEVELOPMENT.md created with developer guide
- [ ] All commands use Vite (not Vue CLI)
</verification>

<success_criteria>
- README.md has accurate quick start for Vue 3 + Vite
- CHANGELOG.md documents all v3 migration changes
- Breaking changes have migration guidance
- Developer documentation comprehensive enough for onboarding
- Documentation matches actual project state
</success_criteria>

<output>
After completion, create `.planning/phases/17-cleanup-polish/17-08-SUMMARY.md`
</output>
