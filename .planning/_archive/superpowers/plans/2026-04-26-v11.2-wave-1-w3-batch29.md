# v11.2 Wave 1 — W3: Issue #29 Entity-Batch Grouping Fix

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix issue #29 so re-review batch creation keeps all entities for a given gene atomic — entities for genes like GNAO1, GRIN1, IFT172 no longer split across two batches when `LIMIT` cuts mid-gene. Add a pre-create UI hint in `ManageReReview.vue` that warns when criteria would extend the batch beyond `batch_size` to keep a gene atomic.

**Architecture:** Extract a shared `select_matching_entities()` helper in `re-review-service.R` that implements the gene-first SELECT + soft-LIMIT algorithm. `batch_preview` and `batch_create` both call it. The preview response gains three fields (`boundary_gene`, `gene_count`, `entity_count`); `ManageReReview.vue` reads `boundary_gene` and renders a `BAlert variant="warning"` when non-null.

**Tech Stack:** R/Plumber + `testthat` (backend), Vue 3 Options API + Vitest + MSW (frontend hint), MySQL 8.

**Worktree:** `/home/bernt-popp/development/sysndd-fh-w3-batch29/`
**Branch:** `fh/batch29-grouping` (off `feature/v11.2-monolith-cleanup` at tag `mc-wave-1-start`)
**Owner:** W3 owner

**Spec ref:** § 4 W3 of `.planning/superpowers/specs/2026-04-25-v11.2-monolith-cleanup-design.md`.

**Forbiddens:**

- Backend touches limited to `api/services/re-review-service.R`, the endpoint that exposes `batch_preview` (verify in W3.1), and `api/tests/testthat/test-re-review-service*.R`.
- Frontend touches limited to `app/src/views/curate/ManageReReview.vue` and any new spec file the UI hint requires.
- Never alter the public response shape of `batch_create` (only `batch_preview` gets new fields).
- Never write a DB migration that mutates existing `re_review_entity_connect` rows — algorithm-only fix per spec.

---

## Background: read once

```bash
cd /home/bernt-popp/development/sysndd-fh-w3-batch29

# Backend — service layer (the bug lives here)
sed -n '120,170p' api/services/re-review-service.R   # batch_preview SELECT
sed -n '244,300p' api/services/re-review-service.R   # batch_create SELECT (duplicate)

# Find the endpoint that exposes batch_preview to see the response wire shape
grep -RnE "batch_preview\(|preview_re_review|/api/re_review/.*preview" api/endpoints/ api/services/ | head

# Frontend — preview consumer
grep -nE "preview" app/src/views/curate/ManageReReview.vue | head -20
grep -nE "previewReReviewBatch|batch_preview" app/src/api/re_review.ts | head -10

# Existing test file location
ls api/tests/testthat/ | grep -i "re-review\|reReview"
```

---

## Task W3.1: Worktree setup + verify the bug exists

**Files:** none (setup + reproduction)

- [ ] **Step 1: Create the worktree**

```bash
cd /home/bernt-popp/development
git -C sysndd worktree add ../sysndd-fh-w3-batch29 feature/v11.2-monolith-cleanup
cd /home/bernt-popp/development/sysndd-fh-w3-batch29
git checkout -b fh/batch29-grouping mc-wave-1-start
```

- [ ] **Step 2: Verify clean tree and starting point**

```bash
git rev-parse --abbrev-ref HEAD                # fh/batch29-grouping
git status --short                             # empty
```

- [ ] **Step 3: Locate the endpoint that exposes `batch_preview`**

```bash
grep -RnE "batch_preview" api/endpoints/ | head
# Expected: a Plumber endpoint file references `batch_preview(criteria, ...)`
```

Note the endpoint file path — it's the file you may need to touch in W3.4 (only if the response structure passthrough requires a wrapper change).

- [ ] **Step 4: Run baseline tests**

```bash
cd /home/bernt-popp/development/sysndd-fh-w3-batch29
make lint-api                         # green baseline
docker compose ps                     # confirm sysndd-api-1 is running

docker exec sysndd-api-1 Rscript -e "testthat::test_dir('/app/tests/testthat', filter = 're-review-service')" 2>&1 | tail -40
# Expected: existing tests pass (none of them currently catch issue #29 — that's why we're here)
```

If the tests directory is not bind-mounted (per AGENTS.md), copy them in first:

```bash
docker cp api/tests/testthat sysndd-api-1:/app/tests/
```

- [ ] **Step 5: Commit nothing**

Setup-only task. Proceed to W3.2.

---

## Task W3.2: Write the failing test for split-gene scenario

**Files:**
- Modify (or create): `api/tests/testthat/test-re-review-service.R`

- [ ] **Step 1: Inspect the existing test file**

```bash
ls api/tests/testthat/test-re-review-service*.R
# If present: grep for 'batch_preview' to see existing patterns we can mirror
grep -n "batch_preview\|batch_create" api/tests/testthat/test-re-review-service.R | head
```

If `test-re-review-service.R` does not exist, create it with the standard `testthat` header. If it exists, append the new test block.

- [ ] **Step 2: Add the failing test**

The test asserts: when criteria match 6 entities across 2 genes (3 each), and `batch_size = 4`, the result must include either 3 (all of gene A) or 6 (all of both genes), never 4 (which would split a gene).

```r
# api/tests/testthat/test-re-review-service.R (append)

context("re-review-service: gene-atomic batching (issue #29)")

test_that("batch_preview keeps all entities for a single gene in the same batch", {
  skip_if_not(exists("get_test_pool"), "test pool helper not available")
  pool <- get_test_pool()
  on.exit(seed_re_review_clear(pool), add = TRUE)

  # Seed: two genes, three entities each — six entities total, all matching the date range.
  seed_re_review_two_genes_three_entities_each(pool)

  criteria <- list(
    date_range = list(start = "2020-01-01", end = "2030-12-31"),
    batch_size = 4
  )
  result <- batch_preview(criteria, batch_size = 4, pool = pool)

  expect_equal(result$status, 200)
  ent_count <- nrow(result$data)
  # Atomicity: never 4 (would split a gene). Must be 3 (one full gene) or 6 (both genes).
  expect_true(ent_count %in% c(3L, 6L),
              info = paste("Expected gene-atomic count in {3, 6}; got", ent_count))

  # All hgnc_id values returned should appear with their full entity count from the seed:
  per_gene <- table(result$data$hgnc_id)
  expect_true(all(per_gene == 3L),
              info = paste("Expected 3 entities per gene; got", paste(per_gene, collapse = ",")))
})
```

If `seed_re_review_two_genes_three_entities_each(pool)` and `get_test_pool()` / `seed_re_review_clear(pool)` helpers don't exist yet, add them to the same file (or a `helper-re-review.R` next to it). They should:

```r
# Helpers (place in helper-re-review.R or top of the test file)
seed_re_review_clear <- function(pool) {
  DBI::dbExecute(pool, "DELETE FROM re_review_entity_connect WHERE re_review_batch >= 99000")
  DBI::dbExecute(pool, "DELETE FROM ndd_entity WHERE entity_id BETWEEN 990001 AND 990999")
}

seed_re_review_two_genes_three_entities_each <- function(pool) {
  # Insert two synthetic genes (HGNC IDs in a high-numbered range to avoid collision) — adapt to schema.
  # Insert three entities for each gene — vary disease ontology to keep the entity unique.
  # Insert review rows with distinct review_date values per entity (this is the bug surface — they drift).
  # Insert active status rows.
  # The exact INSERTs depend on `ndd_entity_view`'s underlying tables — inspect the schema and adapt.
}
```

Adapt the helper INSERTs to match the actual schema of `ndd_entity`, `ndd_entity_review`, `ndd_entity_status`. The existing test file (if any) shows the pattern.

- [ ] **Step 3: Copy tests into the container if not bind-mounted**

```bash
docker cp api/tests/testthat/test-re-review-service.R sysndd-api-1:/app/tests/testthat/
docker cp api/tests/testthat/helper-re-review.R sysndd-api-1:/app/tests/testthat/ 2>/dev/null || true
```

- [ ] **Step 4: Run — fails because the algorithm still splits**

```bash
docker exec sysndd-api-1 Rscript -e "testthat::test_file('/app/tests/testthat/test-re-review-service.R')" 2>&1 | tail -30
# Expected: the new test fails with "Expected gene-atomic count in {3, 6}; got 4" (or similar)
```

If the test errors out before reaching the assertion (e.g. seed helper fails because schema differs), iterate on the helper until the test reaches the assertion.

- [ ] **Step 5: Commit the failing test**

```bash
cd /home/bernt-popp/development/sysndd-fh-w3-batch29
git add api/tests/testthat/test-re-review-service.R api/tests/testthat/helper-re-review.R 2>/dev/null
git commit -m "test(re-review): pin gene-atomic batch behavior (failing — bug #29)"
```

The "failing test committed" pattern lets the next commit's diff show exactly what fixed the bug.

---

## Task W3.3: Implement the gene-first algorithm in a shared helper

**Files:**
- Modify: `api/services/re-review-service.R`

- [ ] **Step 1: Add the shared helper near the top of the service file**

Insert immediately after `build_batch_params`:

```r
#' Select entities for a re-review batch with gene-atomic grouping.
#'
#' Two-step query: first collect distinct hgnc_ids matching criteria
#' (ordered by oldest review_date), then expand to all matching entities
#' for those genes. Trim with a soft LIMIT — include all entities for a
#' partially-included gene; stop adding new genes once cumulative entity
#' count >= batch_size. Returns up to `batch_size` entities, with the
#' boundary gene fully included even if it pushes the count past the cap.
#'
#' @param criteria Named list with batch criteria.
#' @param batch_size Integer cap (default 20). The actual returned count
#'   may exceed this by at most (entities_in_boundary_gene - 1).
#' @param conn Database connection (pool or transaction connection).
#' @return Named list:
#'   - entities: data frame of selected entities with entity_id, hgnc_id,
#'     symbol, status_id, review_id, review_date, etc.
#'   - boundary_gene: hgnc_id where the soft-LIMIT engaged, or NA if the
#'     batch fits cleanly under batch_size.
#'   - gene_count: number of distinct hgnc_id values in the result.
#'   - entity_count: total number of entities (== nrow(entities)).
#'
#' @keywords internal
select_matching_entities <- function(criteria, batch_size = 20, conn) {
  where_clause <- build_batch_where_clause(criteria, conn)
  params <- build_batch_params(criteria)

  # Step 1: distinct hgnc_ids matching the criteria, ordered by oldest review_date per gene.
  # Cap at batch_size (worst case 1 entity per gene = batch_size genes).
  gene_query <- paste0(
    "SELECT e.hgnc_id, MIN(r.review_date) AS first_review_date
     FROM ndd_entity_view e
     LEFT JOIN ndd_entity_review r ON e.entity_id = r.entity_id AND r.is_primary = 1
     LEFT JOIN ndd_entity_status s ON e.entity_id = s.entity_id AND s.is_active = 1
     WHERE ", where_clause, "
       AND e.entity_id NOT IN (
         SELECT rec.entity_id FROM re_review_entity_connect rec
         INNER JOIN re_review_assignment ra ON rec.re_review_batch = ra.re_review_batch
         WHERE rec.re_review_approved = 0
       )
     GROUP BY e.hgnc_id
     ORDER BY first_review_date ASC
     LIMIT ?"
  )
  gene_params <- c(params, list(as.integer(batch_size)))
  gene_rows <- db_execute_query(gene_query, gene_params, conn = conn)

  if (nrow(gene_rows) == 0) {
    return(list(
      entities = data.frame(),
      boundary_gene = NA_character_,
      gene_count = 0L,
      entity_count = 0L
    ))
  }

  # Step 2: pull all matching entities for those genes.
  hgnc_placeholders <- paste(rep("?", nrow(gene_rows)), collapse = ", ")
  ent_query <- paste0(
    "SELECT DISTINCT e.entity_id, e.hgnc_id, e.symbol, e.disease_ontology_name,
            e.disease_ontology_id_version, e.hpo_mode_of_inheritance_term_name,
            r.review_date, r.review_id, s.category_id, s.status_id
     FROM ndd_entity_view e
     LEFT JOIN ndd_entity_review r ON e.entity_id = r.entity_id AND r.is_primary = 1
     LEFT JOIN ndd_entity_status s ON e.entity_id = s.entity_id AND s.is_active = 1
     WHERE ", where_clause, "
       AND e.hgnc_id IN (", hgnc_placeholders, ")
       AND e.entity_id NOT IN (
         SELECT rec.entity_id FROM re_review_entity_connect rec
         INNER JOIN re_review_assignment ra ON rec.re_review_batch = ra.re_review_batch
         WHERE rec.re_review_approved = 0
       )
     ORDER BY e.hgnc_id, r.review_date ASC"
  )
  ent_params <- c(params, as.list(gene_rows$hgnc_id))
  all_matching <- db_execute_query(ent_query, ent_params, conn = conn)

  if (nrow(all_matching) == 0) {
    return(list(
      entities = data.frame(),
      boundary_gene = NA_character_,
      gene_count = 0L,
      entity_count = 0L
    ))
  }

  # Step 3: soft LIMIT — accumulate by gene; stop adding new genes once entity count >= batch_size.
  # The boundary gene is fully included even if it pushes the cumulative count past the cap.
  selected <- list()
  per_gene <- split(all_matching, all_matching$hgnc_id)
  # Preserve gene order as discovered in step 1 (oldest first):
  per_gene <- per_gene[as.character(gene_rows$hgnc_id)]
  total <- 0L
  boundary <- NA_character_
  for (gid in names(per_gene)) {
    block <- per_gene[[gid]]
    if (total >= batch_size) break
    selected[[length(selected) + 1L]] <- block
    total <- total + nrow(block)
    if (total > batch_size && is.na(boundary)) {
      boundary <- gid
    }
  }

  out <- if (length(selected) > 0L) do.call(rbind, selected) else data.frame()
  list(
    entities = out,
    boundary_gene = boundary,
    gene_count = length(selected),
    entity_count = nrow(out)
  )
}
```

- [ ] **Step 2: Update `batch_preview` to consume the helper**

Replace the existing query body in `batch_preview` (the `query <- paste0(...)`, `params <- c(params, list(as.integer(batch_size)))`, `matching_entities <- db_execute_query(...)` block) with:

```r
# In batch_preview, replace the SELECT/LIMIT block with:
selection <- select_matching_entities(criteria, batch_size = batch_size, conn = pool)
matching_entities <- selection$entities

logger::log_info("Batch preview",
  criteria_count = length(criteria),
  matching_count = selection$entity_count,
  gene_count = selection$gene_count,
  boundary_gene = selection$boundary_gene
)

list(
  status = 200,
  data = matching_entities,
  boundary_gene = selection$boundary_gene,
  gene_count = selection$gene_count,
  entity_count = selection$entity_count
)
```

- [ ] **Step 3: Update `batch_create` to consume the helper inside the transaction**

Replace the duplicate `query <- paste0(...)`/`matching_entities <- db_execute_query(...)` block in `batch_create` with:

```r
# Inside batch_create's db_with_transaction(function(txn_conn) { ... }):
selection <- select_matching_entities(criteria, batch_size = batch_size, conn = txn_conn)
matching_entities <- selection$entities

if (nrow(matching_entities) == 0) {
  stop("No entities match the specified criteria")
}

# (existing INSERT loop on `matching_entities` continues unchanged)
```

The downstream INSERT loop iterates `matching_entities` row-by-row to populate `re_review_entity_connect`. The new helper returns the same column shape (`entity_id`, `status_id`, `review_id`), so the loop continues to work without changes.

- [ ] **Step 4: Restart the API to pick up the source change (bind-mounted)**

```bash
docker compose restart api
docker compose logs api --tail 20
# Expected: clean startup
```

- [ ] **Step 5: Re-run the test from W3.2 — should now pass**

```bash
docker exec sysndd-api-1 Rscript -e "testthat::test_file('/app/tests/testthat/test-re-review-service.R')" 2>&1 | tail -30
# Expected: green
```

If still red, inspect the SQL produced by `select_matching_entities` against the seed data — most likely cause is a column-name drift in `ndd_entity_view` vs. the helper's projection.

- [ ] **Step 6: Add a second test for the boundary_gene field**

Append to `test-re-review-service.R`:

```r
test_that("batch_preview surfaces boundary_gene when soft-LIMIT engages", {
  skip_if_not(exists("get_test_pool"), "test pool helper not available")
  pool <- get_test_pool()
  on.exit(seed_re_review_clear(pool), add = TRUE)

  # Seed: 2 genes × 3 entities each. With batch_size=4, the algorithm includes
  # gene A (3 entities) then sees gene B is needed, includes all of B (total 6),
  # boundary_gene is set to gene B.
  seed_re_review_two_genes_three_entities_each(pool)

  result <- batch_preview(list(date_range = list(start = "2020-01-01", end = "2030-12-31")),
                          batch_size = 4, pool = pool)

  expect_equal(result$status, 200)
  expect_equal(result$gene_count, 2)
  expect_equal(result$entity_count, 6)
  expect_false(is.na(result$boundary_gene))
})

test_that("batch_preview returns NA boundary_gene when batch fits cleanly", {
  skip_if_not(exists("get_test_pool"), "test pool helper not available")
  pool <- get_test_pool()
  on.exit(seed_re_review_clear(pool), add = TRUE)

  seed_re_review_two_genes_three_entities_each(pool)

  result <- batch_preview(list(date_range = list(start = "2020-01-01", end = "2030-12-31")),
                          batch_size = 20, pool = pool)

  expect_equal(result$status, 200)
  expect_equal(result$gene_count, 2)
  expect_equal(result$entity_count, 6)
  expect_true(is.na(result$boundary_gene))
})
```

- [ ] **Step 7: Run all `re-review` tests — green**

```bash
docker cp api/tests/testthat/test-re-review-service.R sysndd-api-1:/app/tests/testthat/
docker exec sysndd-api-1 Rscript -e "testthat::test_file('/app/tests/testthat/test-re-review-service.R')" 2>&1 | tail -30
# Expected: green
```

- [ ] **Step 8: Lint API**

```bash
cd /home/bernt-popp/development/sysndd-fh-w3-batch29
make lint-api
# Expected: zero issues
```

- [ ] **Step 9: Commit**

```bash
git add api/services/re-review-service.R api/tests/testthat/test-re-review-service.R
git commit -m "fix(api/re-review): gene-atomic batching with soft LIMIT (closes #29)

Previously batch_preview / batch_create used DISTINCT entity_id with
ORDER BY review_date + LIMIT, which split entities for a gene whose
review dates straddled the LIMIT cutoff (e.g. GNAO1, GRIN1, IFT172).

Replaces the per-call SELECT with a shared select_matching_entities()
helper that does:

  1. SELECT distinct hgnc_ids matching criteria, ordered by oldest
     review_date per gene, capped at batch_size.
  2. SELECT all matching entities WHERE hgnc_id IN (the gene list).
  3. Trim with a soft LIMIT — include all entities for a partially-
     included gene; stop adding new genes once cumulative count >=
     batch_size. The boundary gene is fully included.

batch_preview's response gains boundary_gene, gene_count, entity_count.
batch_create response shape is unchanged."
```

---

## Task W3.4: Confirm the endpoint passthrough (no work expected)

**Files:** likely none

`batch_preview`'s caller in `api/endpoints/re_review_endpoints.R` (or wherever) returns the named list directly to Plumber. The new `boundary_gene` / `gene_count` / `entity_count` fields ride along automatically.

- [ ] **Step 1: Verify the endpoint wrapper passes the fields through**

```bash
grep -RnE "batch_preview\(" api/endpoints/ api/services/
# Expected: a call site like `result <- batch_preview(criteria, batch_size, pool)`
# followed by `return(result)` or similar.
```

If the endpoint constructs its own response shape (e.g. cherry-picks `result$data` and `result$status`), patch it to also pass `boundary_gene`, `gene_count`, `entity_count`. If it returns the result directly, no change.

- [ ] **Step 2: Test the end-to-end response by hitting the dev API**

```bash
# Get a session token (adapt to your dev creds)
TOKEN=$(curl -s -X POST 'http://localhost:8080/api/auth/authenticate' \
  -H 'Content-Type: application/json' \
  -d '{"user_name":"admin","password":"admin"}' | tr -d '"')

curl -s 'http://localhost:8080/api/re_review/preview?criteria=...&batch_size=4' \
  -H "Authorization: Bearer $TOKEN" | jq '{status, gene_count, entity_count, boundary_gene}'
# Expected: object with all four keys present.
```

- [ ] **Step 3: If the endpoint needed a patch, commit it**

```bash
git add api/endpoints/<file>
git commit -m "feat(api/re-review): expose boundary_gene, gene_count, entity_count from batch_preview"
```

---

## Task W3.5: Frontend — update the `previewReReviewBatch` typed-client interface

**Files:**
- Read-only: `app/src/api/re_review.ts` (do not edit per forbiddens — it's owned by v11.1's W3)

> **Important:** The forbiddens forbid edits to `app/src/api/*.ts`. The new fields ride along through the typed-client return type either:
>
> - automatically (if the existing return type is `unknown` or `Record<string, unknown>`), or
> - the consumer in `ManageReReview.vue` reads them via type assertion at call-site
>
> Inspect `app/src/api/re_review.ts:289` (`previewReReviewBatch`) to determine which.

- [ ] **Step 1: Inspect the existing typed-client signature**

```bash
sed -n '280,310p' app/src/api/re_review.ts
# Note the return type. If it's `Promise<unknown>` or `Promise<Record<string, unknown>>`,
# we can read boundary_gene at the call site without changing the client.
# If it's a strongly-typed interface that does NOT include the new fields, we'll need to
# read via type assertion in the view: `(response as any).boundary_gene`.
```

- [ ] **Step 2: Note the path forward in the W3.6 commit message**

If type adjustment is needed, the right place is a follow-up touch-up phase (the smaller-cleanups list). For W3, the view consumes via type assertion — documented in the W3.6 commit message and the view's inline doc.

---

## Task W3.6: Frontend — render the warning hint in `ManageReReview.vue`

**Files:**
- Modify: `app/src/views/curate/ManageReReview.vue`
- Modify or create: `app/src/views/curate/ManageReReview.spec.ts`

- [ ] **Step 1: Locate the preview consumer**

```bash
grep -n "previewReReviewBatch\|batch_preview\|preview" app/src/views/curate/ManageReReview.vue | head -20
# Find where the preview response is stored (e.g. `this.previewResult = response`).
```

- [ ] **Step 2: Wire boundary_gene into a computed**

Add (or extend) a `data()` field and computed to capture the new field:

```ts
// In ManageReReview.vue's data():
previewBoundaryGene: null,    // string | null — non-null when a gene is partial-included
previewGeneCount: 0,
previewEntityCount: 0,

// In the preview-success handler, after storing response:
const result = response as any;
this.previewBoundaryGene = result.boundary_gene ?? null;
this.previewGeneCount = result.gene_count ?? 0;
this.previewEntityCount = result.entity_count ?? 0;
```

Add a computed near the others:

```ts
// In computed:
boundaryGeneAlertVisible() {
  return Boolean(this.previewBoundaryGene);
},
boundaryGeneAlertMessage() {
  if (!this.previewBoundaryGene) return '';
  return `Batch is gene-atomic. To keep gene ${this.previewBoundaryGene} together, the batch will hold ${this.previewEntityCount} entities (you requested ${this.requestedBatchSize}). The next batch will pick up the remaining matches if any. Tighten criteria to avoid the overflow.`;
},
```

If the existing template uses `requested_batch_size` or `batch_size` under a different name, adapt the computed to read whatever the user-input cap is bound to.

- [ ] **Step 3: Render the `BAlert` in the template**

Find the create-batch button block and add the alert immediately above it:

```vue
<!-- inside ManageReReview.vue's <template>, near the create-batch button -->
<BAlert
  v-if="boundaryGeneAlertVisible"
  variant="warning"
  show
  class="mb-2"
  data-testid="batch-boundary-gene-alert"
>
  <i class="bi bi-exclamation-triangle"></i>
  {{ boundaryGeneAlertMessage }}
</BAlert>
```

- [ ] **Step 4: Add a Vitest spec for the hint**

Inspect `ManageReReview.spec.ts` (or create one) and add:

```ts
// app/src/views/curate/ManageReReview.spec.ts (snippet)
import { describe, expect, test } from 'vitest';
import { mount } from '@vue/test-utils';
import ManageReReview from './ManageReReview.vue';

describe('ManageReReview — boundary-gene alert (issue #29)', () => {
  test('alert hidden when previewBoundaryGene is null', async () => {
    const wrapper = mount(ManageReReview, {
      data() {
        return {
          previewBoundaryGene: null,
          previewGeneCount: 0,
          previewEntityCount: 0,
        };
      },
      // ... existing global stubs / mocks the file already establishes
    });
    expect(wrapper.find('[data-testid="batch-boundary-gene-alert"]').exists()).toBe(false);
  });

  test('alert renders when previewBoundaryGene is non-null', async () => {
    const wrapper = mount(ManageReReview, {
      data() {
        return {
          previewBoundaryGene: 'HGNC:4585',
          previewGeneCount: 2,
          previewEntityCount: 6,
          requestedBatchSize: 4,
        };
      },
      // ...
    });
    const alert = wrapper.find('[data-testid="batch-boundary-gene-alert"]');
    expect(alert.exists()).toBe(true);
    expect(alert.text()).toContain('HGNC:4585');
    expect(alert.text()).toContain('6 entities');
  });
});
```

The exact mount harness (mocks, plugins) must match the rest of `ManageReReview.spec.ts`. If there's no existing spec, model the harness after `ApproveReview.spec.ts` (a sibling file with the same global stubs).

- [ ] **Step 5: Run lint + the new spec**

```bash
cd app
npm run lint -- src/views/curate/ManageReReview.vue src/views/curate/ManageReReview.spec.ts
npx vitest run src/views/curate/ManageReReview.spec.ts
# Expected: green
```

- [ ] **Step 6: Commit**

```bash
cd /home/bernt-popp/development/sysndd-fh-w3-batch29
git add app/src/views/curate/ManageReReview.vue app/src/views/curate/ManageReReview.spec.ts
git commit -m "feat(views/curate/ManageReReview): warn when batch criteria would split a gene

Reads boundary_gene from batch_preview and renders a BAlert
warning when soft-LIMIT engages. Read via type assertion (the
typed-client interface in @/api/re_review.ts is owned by another
phase; full type plumbing is a follow-up cleanup)."
```

---

## Task W3.7: Verification + push

**Files:** none

- [ ] **Step 1: Comprehensive checks**

```bash
cd /home/bernt-popp/development/sysndd-fh-w3-batch29

grep -n "boundary_gene" api/services/re-review-service.R                   # > 0
grep -n "boundary_gene" app/src/views/curate/ManageReReview.vue            # > 0
grep -n "split.*gene\|gene-atomic" api/tests/testthat/test-re-review-service.R   # > 0

grep -n "select_matching_entities" api/services/re-review-service.R | head
# Expected: definition + at least 2 call sites (batch_preview, batch_create)
```

- [ ] **Step 2: API tests**

```bash
docker cp api/tests/testthat/test-re-review-service.R sysndd-api-1:/app/tests/testthat/
docker exec sysndd-api-1 Rscript -e "testthat::test_dir('/app/tests/testthat', filter = 're-review-service')" 2>&1 | tail -30
# Expected: green
```

- [ ] **Step 3: API lint + frontend lint + frontend type-check + frontend Vitest**

```bash
cd /home/bernt-popp/development/sysndd-fh-w3-batch29
make lint-api
cd app
npm run lint
npm run type-check
npm run test:unit -- --run
```

- [ ] **Step 4: Optional Playwright smoke**

```bash
cd /home/bernt-popp/development/sysndd-fh-w3-batch29
make playwright-stack
cd app && npx playwright test views.smoke.spec.ts
make playwright-stack-down
# Expected: green; no manage-re-review-specific spec exists today, smoke is sufficient.
```

- [ ] **Step 5: ci-local**

```bash
cd /home/bernt-popp/development/sysndd-fh-w3-batch29
make pre-commit
make ci-local
# Expected: green
```

- [ ] **Step 6: Push and CI**

```bash
git push -u origin fh/batch29-grouping
gh run watch
```

- [ ] **Step 7: Hand off**

Notify the integration owner that `fh/batch29-grouping` is ready to merge.

---

## W3 Self-Review Checklist

- [ ] Failing test `batch_preview keeps all entities for a single gene in the same batch` was committed before the fix
- [ ] `select_matching_entities` helper exists in `api/services/re-review-service.R`
- [ ] `batch_preview` and `batch_create` both call the new helper (no SELECT duplication)
- [ ] `batch_preview` response includes `boundary_gene`, `gene_count`, `entity_count`
- [ ] `batch_create` response shape is unchanged
- [ ] No DB migration created
- [ ] All re-review-service testthat tests green
- [ ] `ManageReReview.vue` renders `BAlert` when `boundary_gene` is non-null
- [ ] `ManageReReview.spec.ts` covers visible / hidden cases
- [ ] `make lint-api` green
- [ ] `make ci-local` green
- [ ] CI green on `fh/batch29-grouping`
