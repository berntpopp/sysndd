# database config file
config.yml
# R files
.Rhistory

# Ignore the results directory in api folder
results/*.json
results/ontology/*.csv

# Ignore the files in the data directory in api folder but not subdirectories
data/*.gz
data/*.txt
data/*.csv
data/*.obo

# Ignore the log files in logs directory in api folder
logs/*.log

# Ignore cache directory (external API cache, Docker volume mount)
cache/

# ignore the development start script
*_development.R

# renv library cache (regenerated via renv::restore())
# Note: renv/library/ etc. are handled by renv/.gitignore
# Additional patterns for safety:
renv/library/
renv/local/
renv/cellar/
renv/lock/
renv/python/
renv/sandbox/
renv/staging/

# User-specific R profile (not project config)
.Rprofile.user