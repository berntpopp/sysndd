// assets/js/classes/submission/submissionSubmission.js

class Submission {
  constructor(entity, review, status) {
    this.entity = entity;
    this.review = review;
    this.status = status;
  }
}

export default {
  Submission,
};
